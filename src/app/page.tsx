// Placeholder only — the real chat shell (welcome, composer, header
// controls) is Plan 01-12. This exists so the dev server has something to
// serve while the Walking Skeleton proves the stack boots end to end.
export default function Page() {
  return <h1>One Chat</h1>;
}
