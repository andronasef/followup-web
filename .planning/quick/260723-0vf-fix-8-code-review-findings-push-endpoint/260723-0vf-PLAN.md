---
phase: quick-260723-0vf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/repo/pushSubscriptions.ts
  - src/server/repo/pushSubscriptions.test.ts
  - src/server/repo/visitors.ts
  - src/server/repo/visitors.test.ts
  - src/app/api/chat/prefs/route.ts
  - src/server/push/subscribe.ts
  - src/server/push/subscribe.test.ts
  - src/server/push/send.ts
  - src/server/push/send.test.ts
  - src/lib/push/subscribe-client.ts
  - src/lib/push/subscribe-client.test.ts
  - src/components/chat/Gate.tsx
  - public/sw.js
  - src/server/realtime/sse-pump.ts
  - src/server/realtime/sse-pump.test.ts
  - src/server/realtime/hub.ts
  - src/server/realtime/hub.test.ts
  - src/app/api/chat/stream/route.ts
  - src/app/api/admin/stream/route.ts
  - src/server/http/ip.ts
  - src/server/http/ip.test.ts
  - src/app/api/chat/messages/send.ts
  - src/app/api/admin/login/login.ts
  - src/app/api/admin/login/login.test.ts
  - src/app/api/admin/login/route.ts
  - src/server/translation/translate.ts
  - src/server/translation/translate.test.ts
  - package.json
autonomous: true
requirements: [CR-01, CR-02, CR-03, CR-04, CR-05, CR-06, CR-07, CR-08]

must_haves:
  truths:
    - "A visitor who clears cookie+localStorage but keeps their push subscription is reunited with their prior conversation; the endpoint's visitor_id is never silently reassigned by the subscribe path (CR-01)."
    - "A visitor who changes their language sees that language survive a cookie-recovery round trip, and the owner's reply to them is translated into it (CR-02)."
    - "Unbounded owner-login attempts are throttled before any Argon2id verify runs, and the throttle never reveals whether an email exists (CR-03)."
    - "A transient DB error inside an SSE pump never produces an unhandled rejection and never kills the container (CR-04)."
    - "A message committed while the SSE backfill pump is still in flight is delivered on that same stream, not deferred to the 4-minute recycle (CR-05)."
    - "A second/late unsubscribe from a torn-down stream never removes the hub registration a freshly reconnected stream just created (CR-06)."
    - "A 4000-codepoint message is translated without truncation, so it neither loses its translation nor drives the circuit breaker open (CR-07)."
    - "Multiple unread replies coalesce into one device notification instead of stacking (CR-08)."
  artifacts:
    - src/server/realtime/sse-pump.ts
    - src/server/realtime/hub.test.ts
    - src/server/http/ip.ts
    - src/app/api/admin/login/login.ts
  key_links:
    - "pushSubscriptions.create() -> subscribe.ts conflict result -> subscribe-client outcome -> Gate.tsx endpoint recovery"
    - "prefs route -> visitors.updatePrefs -> getVisitorLangFor / getVisitorAndLangFor / handleRecover"
    - "hub.subscribe callback -> sse-pump trigger -> repo.messages.since/sinceAll -> SSE send"
    - "admin/login/route.ts -> handleAdminLogin -> ratelimit.check (before verifyPassword)"
---

<objective>
Fix eight verified code-review findings across the push/identity, SSE realtime, security, and translation subsystems, as three atomic commits.

Purpose: two of these are live data-loss/availability bugs (a push-endpoint visitor takeover that permanently orphans a conversation; an unhandled rejection that can kill the single-replica container), and the rest silently degrade the two product guarantees the project is built on — "no message is ever lost" and "always reachable again".

Output: 8 fixed defects, 4 new modules extracted for testability, and node:test coverage for every fix that can be exercised without Next's bundler.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@src/server/repo/pushSubscriptions.ts
@src/server/push/subscribe.ts
@src/server/push/recover.ts
@src/server/push/send.ts
@src/lib/push/subscribe-client.ts
@src/components/chat/Gate.tsx
@src/server/auth/visitor.ts
@src/server/repo/visitors.ts
@src/app/api/chat/prefs/route.ts
@public/sw.js
@src/app/api/chat/stream/route.ts
@src/app/api/admin/stream/route.ts
@src/server/realtime/hub.ts
@src/app/api/admin/login/route.ts
@src/server/repo/ratelimit.ts
@src/app/api/chat/messages/send.ts
@src/server/translation/translate.ts
@src/server/translation/cache.ts
@src/app/api/admin/messages/translate-preview.ts

Established conventions this plan must follow:
- Every route handler that touches `next/headers` is a thin `route.ts` wrapper over a `next/headers`-free behavior module, so `node:test` can import the behavior directly (see send.ts/route.ts, reply.ts/route.ts, recover.ts/route.ts).
- Server modules use explicit `.ts` import extensions so they resolve under plain `node --experimental-strip-types`.
- Repo test files open a real Postgres and close the pool in an `after()` hook.
- Tests are registered by glob in package.json's `test` script; a new test directory must be added there or it never runs.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stop push-endpoint visitor takeover, persist visitor language, and give notifications a real tag (CR-01, CR-02, CR-08)</name>
  <files>src/server/repo/pushSubscriptions.ts, src/server/repo/pushSubscriptions.test.ts, src/server/repo/visitors.ts, src/server/repo/visitors.test.ts, src/app/api/chat/prefs/route.ts, src/server/push/subscribe.ts, src/server/push/subscribe.test.ts, src/server/push/send.ts, src/server/push/send.test.ts, src/lib/push/subscribe-client.ts, src/lib/push/subscribe-client.test.ts, src/components/chat/Gate.tsx, public/sw.js</files>
  <read_first>src/server/repo/pushSubscriptions.ts, src/server/push/subscribe.ts, src/server/push/recover.ts, src/lib/push/subscribe-client.ts, src/components/chat/Gate.tsx, src/server/repo/visitors.ts, src/app/api/chat/prefs/route.ts, src/server/push/send.ts, public/sw.js</read_first>
  <behavior>
    - pushSubscriptions.create(): calling it for an endpoint that already exists under visitor A, passing visitor B, returns the row still owned by visitor A, with p256dh/auth updated to B's supplied values.
    - pushSubscriptions.create(): a fresh endpoint still inserts normally and returns the caller's visitor id.
    - visitors.updatePrefs(): writing a lang for an existing visitor is readable back through the visitors row; a nonexistent visitor id is a silent no-op that returns null rather than inserting.
    - handleSubscribe(): when the endpoint's stored owner differs from the calling visitor id, the result is the conflict status, no probe notification is sent, and no funnel grant row is written.
    - handleSubscribe(): when the endpoint is fresh or already owned by the calling visitor, behavior is unchanged (probe attempted, grant recorded, 200).
    - buildContentFreePayload(): the returned payload carries a top-level tag equal to the caller-supplied value, alongside the existing fixed locale title/body and the signed vid — and still carries no message text, name, email, or phone.
    - syncSubscriptionOnOpen(): a 409 response resolves to the conflict outcome; a 2xx resolves to the ok outcome; no subscription at all resolves to the skipped outcome; a thrown error resolves to the failed outcome and never rejects.
  </behavior>
  <action>
CR-01 (critical, data loss). In `src/server/repo/pushSubscriptions.ts`, change `create()`'s `onConflictDoUpdate` so the conflict branch updates only the encryption keys and never touches the owning visitor column — the endpoint-to-visitor binding becomes set-once at insert. Update the module header note to state that the binding is set-once and that `getByEndpoint()`/`handleRecover()` depend on it. Then in `src/server/push/subscribe.ts`, read the row `create()` returns and compare its owning visitor id to `input.visitorId`; when they differ, return a new `{ status: 409, body: { error: "endpoint_owned_by_other_visitor" } }` variant added to `HandleSubscribeResult` — returning BEFORE the probe send and BEFORE `gateFunnel.recordGranted`, so a mistakenly-minted visitor neither fires a spurious notification nor pollutes the funnel. Leave the fresh/same-owner path byte-for-byte unchanged.

Close the loop client-side. In `src/lib/push/subscribe-client.ts`, export a `SubscribeOutcome` union (ok / conflict / skipped / failed) and change `syncSubscriptionOnOpen` to resolve to that outcome instead of `void` (409 maps to conflict), and `subscribeToPush` to resolve to an object carrying both the outcome and the existing `probeOk` value (a 409 there maps to conflict too). Both must still never throw. In `src/components/chat/Gate.tsx`, collapse the two concurrent mount IIFEs into ONE sequential async IIFE and extract the endpoint-recovery body into a local `recoverIdentityByEndpoint()` helper returning a boolean: the recovery attempt runs FIRST (still gated on the `data-cookie-present` marker being the no-cookie value) and reloads and returns on success; only if it did not recover does the PUSH-11 re-sync run; and if that re-sync (or `handleAllow`'s `subscribeToPush`) reports the conflict outcome, call `recoverIdentityByEndpoint()` and reload on success. This makes recovery deterministically win the race instead of winning it by luck, while the set-once binding above makes the outcome safe even if some future caller violates the ordering.

CR-02. Add `updatePrefs(visitorId, lang, appearance)` to `src/server/repo/visitors.ts` — a single UPDATE that sets only the non-null arguments supplied (leave a column untouched when its argument is undefined/null), also touching `lastSeenAt`, returning the updated row or null when no row matched. No schema change: `visitors.lang` and `visitors.appearance` already exist. Call it from `src/app/api/chat/prefs/route.ts`'s PATCH immediately after `requireVisitor()` resolves and before signing the cookie, persisting the resolved next lang and next appearance. This is what makes `getVisitorLangFor()` (translate-preview), `getVisitorAndLangFor()` (reply/push) and `handleRecover()` read the visitor's actual choice instead of the Accept-Language guess frozen at insert.

CR-08. In `src/server/push/send.ts`, give `buildContentFreePayload` a third `tag` parameter and emit it as a top-level `tag` field on the payload (keep `data: { vid }` exactly as-is — the tag is a routing/coalescing key, never message content, so ID-05 is preserved). Pass the same conversation-scoped value already used as the web-push `topic` in `sendPushToVisitor`. Make `src/server/push/subscribe.ts`'s probe stop hand-building its own payload object and call `buildContentFreePayload` with its existing visitor-scoped probe topic value, so the two call sites finally share the one definition the send.ts header already claims they do. In `public/sw.js`, read the tag off the top-level payload field instead of the nonexistent nested conversation id, falling back to no tag when the field is absent or not a string (the non-JSON DevTools-push path); the `showNotification` call stays unconditional.
  </action>
  <verify>
    <automated>node --experimental-strip-types --env-file=.env.local --test src/server/repo/pushSubscriptions.test.ts src/server/repo/visitors.test.ts src/server/push/subscribe.test.ts src/server/push/send.test.ts src/lib/push/subscribe-client.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('public/sw.js','utf8').split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(!/data\.tag/.test(s)) throw new Error('sw.js does not read the payload tag field'); if(/conversationId/.test(s)) throw new Error('sw.js still reads a field the payload never carries');"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/app/api/chat/prefs/route.ts','utf8'); if(!/updatePrefs/.test(s)) throw new Error('prefs route does not persist the chosen lang');"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>An existing push endpoint keeps its original visitor id no matter which visitor re-subscribes it; a conflicting subscribe returns 409 with no probe and no funnel write; the Gate mount effect runs recovery-then-sync sequentially and recovers on a reported conflict; a manual language change is readable back from the visitors row; every push payload carries a tag the service worker actually uses; typecheck is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the SSE pump crash-proof, gap-proof, and the hub unsubscribe identity-checked (CR-04, CR-05, CR-06)</name>
  <files>src/server/realtime/sse-pump.ts, src/server/realtime/sse-pump.test.ts, src/server/realtime/hub.ts, src/server/realtime/hub.test.ts, src/app/api/chat/stream/route.ts, src/app/api/admin/stream/route.ts, package.json</files>
  <read_first>src/app/api/chat/stream/route.ts, src/app/api/admin/stream/route.ts, src/server/realtime/hub.ts, src/server/repo/messages.ts</read_first>
  <behavior>
    - createPump: triggering it while a run is already in flight does not start a second concurrent run; the in-flight run loops again and emits the rows that arrived, so nothing committed during the first pass is dropped.
    - createPump: a trigger that arrives during the very first (backfill) run is honored by that same run — no separate live/not-live gate, no dropped event.
    - createPump: rows are emitted strictly in ascending id order across a trigger storm, with no duplicates, and the high-water mark advances monotonically.
    - createPump: when the row fetcher rejects, the returned promise still resolves (never rejects), the error is handed to the onError callback exactly once per failed run, and a later successful trigger still emits from the last successfully emitted id.
    - hub.subscribe: calling the returned unsubscribe twice is a no-op the second time.
    - hub.subscribe: after subscriber A unsubscribes (emptying and removing the map entry) and a new subscriber B subscribes for the same conversation, a second late unsubscribe call from A does NOT remove B's entry, and a published event still reaches B.
    - hub.subscribeAll: same double-call idempotence.
  </behavior>
  <action>
Extract the duplicated pump into a new `next/headers`-free `src/server/realtime/sse-pump.ts` exporting `createPump(options)` where options carry the starting id, an async `fetchSince(sinceId)` row fetcher, an `emit(row)` callback, and an `onError(error)` callback. Internally it owns the high-water mark, a single-flight in-progress flag, and a re-run flag; `run()` awaits a full drain loop and `trigger()` starts a run or marks the re-run flag. CR-04: wrap the whole drain loop in try/catch so the returned promise NEVER rejects — hand the error to `onError` and reset the in-flight flag in a `finally`, so a transient DB error inside `since()`/`sinceAll()` can no longer become a Node-24 process-killing unhandled rejection. CR-05: the module has no live/not-live concept at all — the re-run flag is the only handoff, so a trigger arriving mid-backfill is always honored by the in-flight run instead of setting an already-true flag that gets discarded.

Rewrite both `src/app/api/chat/stream/route.ts` and `src/app/api/admin/stream/route.ts` to use it: delete the local `highWaterMark` / `live` / `gotEventDuringBackfill` / `pumping` / `dirty` variables and the inline `pump()` declaration; build the pump with `since(conversationId, id)` and `sinceAll(id)` respectively, with `emit` calling the existing `send(row.id, "message", row)` and `onError` logging ids/status only (never message bodies — the pino redaction rule in CLAUDE.md) and then calling `cleanup()` so the client's EventSource reconnects and replays from its `Last-Event-ID`, which is what keeps "no message is ever lost" true across a DB blip. The hub subscription callback now calls `trigger()` unconditionally on a message event. Keep the subscribe-before-backfill ordering, the presence handling, the heartbeat, the 4-minute recycle, and the abort/cancel cleanup exactly as they are; collapse the two sequential awaited backfill calls into the single `run()`.

CR-06. In `src/server/realtime/hub.ts`, make both `subscribe` and `subscribeAll` return an unsubscribe that is guarded by a closure-local already-unsubscribed flag (second and later calls are no-ops), and make `subscribe`'s map cleanup identity-checked: only delete the conversation's map entry when the map still holds the exact Set this closure captured AND that Set is empty. Both guards matter — the stream's abort listener and the ReadableStream `cancel()` both call the same handle, so without them a late second call can delete the registration a freshly reconnected stream just created and silently deafen it.

Create `src/server/realtime/hub.test.ts` and `src/server/realtime/sse-pump.test.ts` covering the behaviors above (both are pure in-memory modules — no DB, no pool teardown hook needed), and add `src/server/realtime/*.test.ts` to package.json's `test` script glob list so they actually run.
  </action>
  <verify>
    <automated>node --experimental-strip-types --test src/server/realtime/hub.test.ts src/server/realtime/sse-pump.test.ts</automated>
    <automated>node -e "for(const f of ['src/app/api/chat/stream/route.ts','src/app/api/admin/stream/route.ts']){const s=require('fs').readFileSync(f,'utf8').split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(/gotEventDuringBackfill/.test(s)) throw new Error(f+' still uses the dropped-event backfill flag'); if(!/createPump/.test(s)) throw new Error(f+' does not use the shared pump');}"</automated>
    <automated>node -e "const s=require('fs').readFileSync('package.json','utf8'); if(!/server\/realtime\/\*\.test\.ts/.test(s)) throw new Error('realtime tests are not registered in the test glob');"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Both SSE routes drive one shared pump whose promise never rejects and whose re-run flag is honored during the initial backfill; a rejecting row fetcher routes to onError and recycles the stream instead of crashing the process; a double or late unsubscribe cannot remove a newer subscriber's hub registration; the new realtime tests run under `npm test`; typecheck is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rate-limit owner login and size the translation token budget to the input (CR-03, CR-07)</name>
  <files>src/server/http/ip.ts, src/server/http/ip.test.ts, src/app/api/chat/messages/send.ts, src/app/api/admin/login/login.ts, src/app/api/admin/login/login.test.ts, src/app/api/admin/login/route.ts, src/server/translation/translate.ts, src/server/translation/translate.test.ts, package.json</files>
  <read_first>src/app/api/admin/login/route.ts, src/server/repo/ratelimit.ts, src/app/api/chat/messages/send.ts, src/server/push/recover.ts, src/app/api/push/recover/route.ts, src/server/translation/translate.ts, src/server/translation/cache.ts</read_first>
  <behavior>
    - handleAdminLogin: with a fresh bucket and correct credentials, returns 200 plus the signed owner-session cookie value for route.ts to set.
    - handleAdminLogin: with a fresh bucket and a wrong password, and with a fresh bucket and an unknown email, returns the identical 401 body — no branch distinguishes them.
    - handleAdminLogin: once the caller's IP bucket is exhausted, returns 429 with a generic body, without performing the responder lookup or any password verification.
    - handleAdminLogin: the bucket key is derived from the hashed IP only, so a wrong-password storm from one IP never locks the owner out from a different IP, and the 429 reveals nothing about whether the email exists.
    - maxTokensFor: a short input yields the floor budget; a 4000-codepoint input yields a budget far above 500 and at or below the hard cap; the value is monotonically non-decreasing in input length and is computed by code point, not UTF-16 code unit.
    - translate(): the max_tokens it passes to the SDK is the value maxTokensFor returns for the supplied text (assert on the mocked create call's arguments).
  </behavior>
  <action>
CR-03. First move `clientIp` and `hashIp` verbatim out of `src/app/api/chat/messages/send.ts` into a new `src/server/http/ip.ts` (keeping the T-01-25 header note that the raw IP is never logged, stored, or notified — only its HMAC), and have `send.ts` import and re-export both so `src/app/api/chat/messages/route.ts` and the existing tests keep working unchanged. The admin login route must not import from the visitor chat send module.

Then split the login route the way every other write route in this repo is split: create a `next/headers`-free `src/app/api/admin/login/login.ts` exporting `handleAdminLogin({ ip, rawBody })` that returns `{ status, body }` plus a `cookieValue` on success (mirror `src/server/push/recover.ts`'s exact result shape), moving the existing zod schema, the module-scope dummy-hash constant, the non-enumerating `invalidCredentials()` convergence, and the `signOwnerSession` call into it — none of that behavior may change. As its FIRST action, before the responder lookup and before any `verifyPassword` call, call the existing `check()` from `src/server/repo/ratelimit.ts` with a bucket key built from the hashed IP under an admin-login-specific key prefix; on a not-allowed result return 429 with a generic body. Do not write a new limiter and do not add a per-email bucket (a per-email bucket would let any attacker lock the single owner out from another IP). Choose a strict but non-punitive capacity/refill pair appropriate to a single human owner typing a password — roughly a ten-attempt burst refilling on the order of one attempt every ten seconds — and export both constants so the test can exhaust the bucket deterministically. Reduce `src/app/api/admin/login/route.ts` to the thin wrapper: parse the body, resolve the caller IP via the new module, call `handleAdminLogin`, set the `owner_session` cookie with its existing options when a cookieValue comes back, and return the body/status. `proxy.ts` needs no change — it never covered `/api/admin/*` and now does not need to.

CR-07. In `src/server/translation/translate.ts`, replace the fixed `max_tokens: 500` with an exported `maxTokensFor(text)` that sizes the budget to the input measured in code points (matching `MAX_MESSAGE_CODEPOINTS`'s own code-point-aware bound of 4000 in `src/app/api/chat/messages/send.ts` and the identical bound in the admin reply path), with a floor for very short inputs, headroom for the JSON envelope, an expansion allowance covering the upper end of the existing `lengthRatioOk` band, and a hard cap so a single call can never run away. Define the constants locally with a comment naming the 4000-codepoint message bound they are derived from — do not import across the server/app layer boundary for this. Pass the computed value as `max_tokens`. Do not retune the four validators or their thresholds; they are corpus-tested and this fix exists precisely so a long message stops failing them by truncation, which today marks the row failed and trips `circuit-breaker.recordFailure()` in `src/server/translation/cache.ts`, suppressing translation for short messages too. The same fix covers the long-owner-draft path through `src/app/api/admin/messages/translate-preview.ts`, which calls `translate()` directly — no change needed there.

Register the two new test directories by adding `src/server/http/*.test.ts` and `src/app/api/admin/login/*.test.ts` to package.json's `test` script glob list.
  </action>
  <verify>
    <automated>node --experimental-strip-types --env-file=.env.local --test src/server/http/ip.test.ts src/app/api/admin/login/login.test.ts src/server/translation/translate.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/server/translation/translate.ts','utf8').split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(/max_tokens:\s*500/.test(s)) throw new Error('translate.ts still uses the fixed token cap');"</automated>
    <automated>node -e "const s=require('fs').readFileSync('package.json','utf8'); for(const g of ['server/http/\\*\\.test\\.ts','admin/login/\\*\\.test\\.ts']){ if(!new RegExp(g).test(s)) throw new Error('missing test glob: '+g);}"</automated>
    <automated>npm test</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Owner login is throttled per hashed IP before any Argon2id verify runs, with the non-enumerating 401 convergence intact and the rate-limit response revealing nothing about account existence; the translation token budget scales with input so a 4000-codepoint message translates without truncation; the whole node:test suite and typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → `/api/push/subscribe` | untrusted, unauthenticated endpoint + keys claim crosses here; it decides which visitor identity a device is bound to |
| anonymous browser → `/api/admin/login` | untrusted credential guesses cross here against the single owner account, each costing a full Argon2id verify |
| anonymous browser → `/api/chat/prefs` | untrusted lang/appearance now reaches a persisted DB column, not just a signed cookie |
| push service → `public/sw.js` | payload-shaped data crosses into the device notification surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0vf-01 | Spoofing | `pushSubscriptions.create` / `handleSubscribe` | critical | mitigate | Endpoint-to-visitor binding becomes set-once; a subscribe by a different visitor returns 409 instead of rebinding, so no caller can take over another visitor's device binding or reach their conversation (Task 1) |
| T-0vf-02 | Denial of Service | `/api/admin/login` | high | mitigate | Per-hashed-IP token bucket checked before the responder lookup and before Argon2id, bounding both credential brute force and CPU pinning of the single replica (Task 3) |
| T-0vf-03 | Denial of Service | SSE pump in both stream routes | high | mitigate | Pump promise never rejects; errors route to onError + deliberate stream recycle, so a transient DB error cannot terminate the Node 24 process and drop every other visitor's stream (Task 2) |
| T-0vf-04 | Information Disclosure | `/api/admin/login` 429 path | medium | mitigate | Rate limit keyed on hashed IP only, never on email, and the 429 body is generic — the throttle adds no account-existence oracle to the existing non-enumerating 401 (Task 3) |
| T-0vf-05 | Information Disclosure | push payload `tag`, SSE `onError` logging | medium | mitigate | The tag carries only a conversation/probe routing key, never message text; onError logs ids and status only, per the CLAUDE.md no-message-bodies-in-logs rule (Tasks 1, 2) |
| T-0vf-06 | Tampering | `visitors.updatePrefs` via `/api/chat/prefs` | low | mitigate | Identity still comes exclusively from the verified cookie via `requireVisitor()`; the body never carries a visitor id, and lang is already validated against `SUPPORTED_LANGUAGES` before the write (Task 1) |
| T-0vf-SC | Tampering | npm/pip/cargo installs | high | accept | No dependency is added or changed by this plan; `package.json` is edited only to extend the `test` script glob, so no package-legitimacy audit surface exists |
</threat_model>

<verification>
- `npm test` passes end to end (requires the local Postgres from docker-compose to be up, per the repo-test convention).
- `npx tsc --noEmit` is clean.
- `npm run build` succeeds — the two SSE routes and `Gate.tsx` were restructured, so the Next build is the only check that covers the client bundle and route-module shape.
- No new runtime dependency appears in `package.json`; no new file appears under `drizzle/` (no schema change was required — `visitors.lang` and `visitors.appearance` already exist).
- Three commits, one per task, each independently passing its own task-level automated verify.
</verification>

<success_criteria>
All eight review findings are closed, each with either a node:test asserting the fixed behavior or a deterministic file assertion where Next's bundler makes a unit test impossible:
1. CR-01 endpoint takeover — repo test proves the binding is set-once; subscribe test proves the 409/no-probe/no-funnel branch.
2. CR-02 lang persistence — repo test proves the write; file assertion proves the prefs route calls it.
3. CR-03 login rate limit — login test proves 429 before any verify, using the existing `check()` helper.
4. CR-04 unhandled rejection — pump test proves the promise resolves and onError fires when the fetcher rejects.
5. CR-05 backfill gap — pump test proves a trigger during the first run is honored by that run.
6. CR-06 unsubscribe — hub test proves a late/second unsubscribe leaves a newer subscriber registered and receiving.
7. CR-07 token budget — translate test proves the budget scales with input and is the value actually passed to the SDK.
8. CR-08 notification tag — send test proves the payload carries a tag; file assertion proves `sw.js` reads it.
</success_criteria>

<output>
Create `.planning/quick/260723-0vf-fix-8-code-review-findings-push-endpoint/260723-0vf-SUMMARY.md` when done
</output>
