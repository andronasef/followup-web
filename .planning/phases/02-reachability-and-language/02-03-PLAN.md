---
phase: 02-reachability-and-language
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/locales/en.json
  - src/lib/i18n/locales/ar.json
  - src/lib/i18n/locales/es.json
  - src/lib/i18n/locales/fr.json
  - src/lib/i18n/locales/pt.json
  - src/lib/i18n/locales/hi.json
  - src/lib/i18n/locales/zh.json
  - src/lib/i18n/locales/ru.json
  - src/lib/i18n/locales/id.json
  - src/lib/i18n/locales/sw.json
autonomous: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-05, PUSH-06, PUSH-07]

must_haves:
  truths:
    - "All 10 locale JSON files carry the identical new key set introduced this phase (a mechanical sorted-key-list diff across all 10 files shows zero divergence), each with a real, human-quality translated string for that language — never an untranslated English placeholder copied into a non-English file."
  artifacts:
    - src/lib/i18n/locales/*.json (17 new keys each, all 10 languages)
  key_links:
    - "Plan 02-07's Gate.tsx/IosWalkthrough.tsx consume pushGateHeading/Body/AllowCta/Confirming/DeclinedHeading/DeclinedBody/RetryCta and the iosWalkthrough* keys via getStrings(lang)."
    - "Plan 02-08's MessageBubble.tsx consumes showOriginalLabel/hideOriginalLabel via getStrings(lang)."
    - "Plan 02-06/02-04's push send payload construction consumes pushNotificationTitle/pushNotificationBody per-language (D-14: authored once, never machine-translated at send time)."
  prohibitions:
    - "MUST NOT use dark-pattern, guilt, or spiritual-rejection framing anywhere in the push gate or re-ask copy that makes declining feel like a punishment or a failure — the tone must stay as gentle and non-coercive as Phase 1's existing rate-limit/welcome copy, even though PUSH-01 makes this gate a hard block on the visitor's first attempt. (Adversarial recall, Stage 2 kept — values concern specific to a pastoral product serving people in a vulnerable moment; not covered by any existing routine-copy check. Authored descriptor-less per the spec-less-fallback's prohibition-authoring rule — flagged for judgment-based review at UI-review time, not a wired mechanical check.)"
---

<objective>
Author every new visitor-facing locale string this phase introduces — push-gate copy, the iOS walkthrough sequence, the visitor-side "See original"/"Hide original" toggle, and the fixed push-notification title/body — into all 10 existing locale JSON files, with real translations (not English placeholders), following the exact tone and register Phase 1's welcome/gate copy already established.

Purpose: doing this as its own Wave 1 plan (rather than folding it into the UI plans that consume these keys) means Plan 02-07 and Plan 02-08 can both depend on a single, already-complete, already-parity-verified set of locale files instead of two UI plans racing to add different keys to the same 10 files in parallel.
Output: 17 new keys added to all 10 locale JSON files (170 translated strings total), with mechanical key-set parity verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-reachability-and-language/02-CONTEXT.md
@.planning/phases/02-reachability-and-language/02-UI-SPEC.md

@src/lib/i18n/locales/en.json
@src/lib/i18n/strings.ts
@src/server/i18n/detect.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push-gate and re-ask copy (7 keys, all 10 languages)</name>
  <files>src/lib/i18n/locales/en.json, ar.json, es.json, fr.json, pt.json, hi.json, zh.json, ru.json, id.json, sw.json</files>
  <read_first>
    src/lib/i18n/locales/en.json (existing key style/casing), 02-UI-SPEC.md's Copywriting Contract table (the exact locked English source strings and locale-key names for pushGateHeading/Body/AllowCta/Confirming/DeclinedHeading/DeclinedBody/RetryCta)
  </read_first>
  <action>
    Add these 7 keys to all 10 locale JSON files, using UI-SPEC.md's Copywriting Contract table's exact English text as the source-of-truth meaning to translate (do not paraphrase the English meaning, only localize it): `pushGateHeading` ("Stay reachable"), `pushGateBody` ("So I can reach you after you close this tab, allow notifications. You'll only ever be told a reply is waiting — nothing about it is shown until you open the chat yourself."), `pushGateAllowCta` ("Allow notifications"), `pushGateConfirming` ("Confirming…"), `pushGateDeclinedHeading` ("Without notifications, I can't reach you again"), `pushGateDeclinedBody` ("If you leave now, you can still open this chat later, but I won't be able to let you know when I've replied. You can still say yes."), `pushGateRetryCta` ("Try again"). Every non-English translation must be a real, natural translation in that language's own register (matching the warm-but-plain tone of Phase 1's existing `welcomeLine1`/`welcomeLine2Online` keys in the same file) — never leave an English string copied verbatim into a non-English file. Keep the re-ask copy gentle: no alarming/destructive framing, no countdown, no "you have been blocked" language, per the kept prohibition above and matching the tone discipline UI-SPEC.md's Copy rules section states explicitly for this exact copy.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "pushGateHeading" src/lib/i18n/locales/*.json` == 1 for every one of the 10 files (7 keys × 10 files all present).
    - Behavior assertion: a Node script parsing all 10 JSON files and diffing their key sets for these 7 keys shows zero missing/extra keys across files.
    - Behavior assertion: none of the 9 non-English files contains the literal English source string for any of the 7 keys (spot-check via grep of the exact English phrases against each non-English file — zero matches expected).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const keys=['pushGateHeading','pushGateBody','pushGateAllowCta','pushGateConfirming','pushGateDeclinedHeading','pushGateDeclinedBody','pushGateRetryCta'];const langs=['en','ar','es','fr','pt','hi','zh','ru','id','sw'];let ok=true;for(const l of langs){const j=JSON.parse(fs.readFileSync('src/lib/i18n/locales/'+l+'.json','utf8'));for(const k of keys){if(!(k in j)){console.error(l,'missing',k);ok=false;}}}process.exit(ok?0:1)"</automated>
  </verify>
  <done>All 10 locale files carry the same 7 new gate/re-ask keys with real, distinctly-non-English translations for the 9 non-English files.</done>
</task>

<task type="auto">
  <name>Task 2: iOS walkthrough + show-original + push-notification copy (10 keys, all 10 languages)</name>
  <files>src/lib/i18n/locales/en.json, ar.json, es.json, fr.json, pt.json, hi.json, zh.json, ru.json, id.json, sw.json</files>
  <read_first>
    02-UI-SPEC.md's Copywriting Contract table (iosWalkthrough* rows, showOriginalLabel/hideOriginalLabel, pushNotificationTitle/Body — exact English source strings)
  </read_first>
  <action>
    Add these 10 keys to all 10 locale JSON files (same file set as Task 1, same real-translation discipline, same tone rules): `iosWalkthroughHeading` ("Add One Chat to your Home Screen first"), `iosWalkthroughIntro` ("iPhone only allows notifications for apps added to your Home Screen. It takes a few seconds."), `iosWalkthroughStep1` ("Tap the Share icon"), `iosWalkthroughStep2` ("Tap \"Add to Home Screen\""), `iosWalkthroughStep3` ("Open One Chat from your Home Screen to continue"), `iosWalkthroughCta` ("I've added it"), `showOriginalLabel` ("See original"), `hideOriginalLabel` ("Hide original"), `pushNotificationTitle` ("You have a new reply"), `pushNotificationBody` ("Tap to open the conversation."). Per D-14 and UI-SPEC's Copy rules, `pushNotificationTitle`/`pushNotificationBody` must contain ZERO visitor-specific content (no name, no message preview, no faith reference, no sender identity) in every language — this is a fixed phrase, not a template; confirm each translation is equally content-free, not just the English source.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "pushNotificationTitle" src/lib/i18n/locales/*.json` == 1 for every one of the 10 files (10 keys × 10 files all present).
    - Behavior assertion: a Node script diffing key sets for these 10 keys across all 10 files shows zero missing/extra keys.
    - Behavior assertion: none of the 9 non-English files contains the literal English source string for any of the 10 keys.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const keys=['iosWalkthroughHeading','iosWalkthroughIntro','iosWalkthroughStep1','iosWalkthroughStep2','iosWalkthroughStep3','iosWalkthroughCta','showOriginalLabel','hideOriginalLabel','pushNotificationTitle','pushNotificationBody'];const langs=['en','ar','es','fr','pt','hi','zh','ru','id','sw'];let ok=true;for(const l of langs){const j=JSON.parse(fs.readFileSync('src/lib/i18n/locales/'+l+'.json','utf8'));for(const k of keys){if(!(k in j)){console.error(l,'missing',k);ok=false;}}}process.exit(ok?0:1)"</automated>
  </verify>
  <done>All 10 locale files carry the same 10 new walkthrough/toggle/notification keys, all 17 new keys from both tasks are present with full 10-language parity, and pushNotificationTitle/Body are verified content-free in every language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (none new) | This plan adds static, developer-authored copy only — no user input, no new runtime code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Information Disclosure | pushNotificationTitle/Body copy | high | mitigate | Every language's translation is explicitly verified content-free (no name/preview/sender/faith-reference) per PUSH-07/D-14 — a mistranslation that accidentally introduces specificity would defeat the whole "content-free push" design. |

</threat_model>

<verification>
- Both automated key-parity/content-free checks above pass for all 10 files.
- `npm run build` succeeds (JSON files remain valid, no syntax errors introduced).
</verification>

<success_criteria>
All 10 locale files carry a complete, parity-verified, real-translation set of the 17 new Phase 2 visitor-facing keys, ready for Plan 02-07 and Plan 02-08 to consume via the existing `getStrings(lang)` lookup with zero further locale-file changes needed this phase.
</success_criteria>

## Artifacts this phase produces (this plan)

- 17 new keys × 10 locale JSON files: `pushGateHeading`, `pushGateBody`, `pushGateAllowCta`, `pushGateConfirming`, `pushGateDeclinedHeading`, `pushGateDeclinedBody`, `pushGateRetryCta`, `iosWalkthroughHeading`, `iosWalkthroughIntro`, `iosWalkthroughStep1`, `iosWalkthroughStep2`, `iosWalkthroughStep3`, `iosWalkthroughCta`, `showOriginalLabel`, `hideOriginalLabel`, `pushNotificationTitle`, `pushNotificationBody`.

## Flagged Assumptions (Edge Probe, specless fallback)

None assigned to this plan — this plan's must-have is an additional LANG-07 parity truth, not one of the 44 probe-report items.

<output>
Create `.planning/phases/02-reachability-and-language/02-03-SUMMARY.md` when done
</output>
