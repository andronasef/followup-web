---
phase: 02-reachability-and-language
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/translation/translate.ts
  - src/server/translation/circuit-breaker.ts
autonomous: true
requirements: [TRANS-07, TRANS-08, TRANS-10]

must_haves:
  truths:
    - "The four TRANS-07 validators (script-block match, length-ratio, refusal-marker detection, emoji/URL/digit token preservation) run against whatever text the model returns, including a degenerate empty-string or single-character response — an empty/too-short response fails the length-ratio check and is treated as invalid output, never accepted as a valid translation."
    - "Script-block validation tests Unicode code-point ranges, not UTF-16 code units or byte length, so multi-byte scripts (Arabic, CJK, Devanagari, Cyrillic) and astral-plane emoji in the source text are validated correctly rather than miscounted or matched against the wrong script range — carried over verbatim from the corpus-tested spike, not re-derived."
    - "An empty or whitespace-only message body never reaches the translation prompt (callers are expected to have already rejected it before persistence, per CHAT-03/TRANS-05); the isolation guarantee — untrusted text confined to its own user message, system prompt as the only instruction channel — applies uniformly to every non-empty body regardless of length."
    - "The structural isolation (system-prompt instruction + isolated user message containing the untrusted text verbatim) does not depend on the source text's script or encoding — an injection attempt written in Arabic, Cyrillic, or any other supported script is isolated identically to one written in Latin script, since the isolation is structural (message-role separation), not content-based filtering or keyword blocking."
    - "The circuit breaker opens after its configured consecutive-failure threshold is reached (not one call before, not one after) and skips calling the provider entirely for the remainder of its cooldown window; every translation attempt while the breaker is open resolves immediately without a network call."
    - { statement: "TRANS-10's numeric retry/cooldown bookkeeping (failure count, cooldown duration) never overflows or mis-rounds at realistic call volumes for a single-owner pastoral chat app", verification: backstop }
  artifacts:
    - src/server/translation/translate.ts
    - src/server/translation/circuit-breaker.ts
  key_links:
    - "translate.ts's translate() function and its exported validators are imported by Plan 02-05's cache.ts and Plan 02-05's translate-preview.ts."
    - "circuit-breaker.ts's isOpen()/recordFailure()/recordSuccess() wrap every call site in Plan 02-05's cache.ts."
  prohibitions:
    - "MUST NOT allow the translation model to inject its own commentary, disclaimers, refusal caveats, or religious/moral editorializing into a translation's output — every word the visitor or owner reads must be a faithful rendering of what the human actually wrote, never an addition, even a well-intentioned one. (Adversarial recall, Stage 2 kept — values/product-trust concern specific to this product's 'a machine never authors a word' core value; not fully covered by TRANS-07's length-ratio/refusal-marker checks, since a short added disclaimer sentence can stay within the length-ratio bound. Authored descriptor-less per the spec-less-fallback's prohibition-authoring rule — flagged for judgment-based review, not a wired mechanical check.)"
---

<objective>
Extract the FOUND-01 spike's corpus-tested translation call and its four validators out of the disposable `scripts/translation-spike.mjs` into a real, importable server module — rebuilt on the actual `openai` SDK (not the spike's hand-rolled fetch client, which existed only so the spike script had zero app-coupling) — plus a small `globalThis`-pinned circuit breaker for TRANS-10.

Purpose: this is the one piece of genuinely new logic (structural prompt isolation, validator tuning) this phase's translation vertical needs, and it must not silently drift from the spike's already-corpus-tested behavior. Building it as its own plan, independent of the schema/repo work in Plan 02-01, keeps this pure-function surface easy to verify (no DB, no network flakiness) before Plan 02-05 wires it into the live message flow.
Output: `src/server/translation/translate.ts` (translate + validators) and `src/server/translation/circuit-breaker.ts` (TRANS-10 breaker).

Per **D-07**, this module is built against `src/server/config/models.ts`'s existing `activeProvider`/`MODEL_ID` exports unchanged — NVIDIA NIM remains the active provider, no switch back to OVH, no rewrite of the config seam. Per **D-08**, no Qwen3.6-27B cost/latency spike is performed this phase; the existing `qwen/qwen3.5-397b-a17b` model id is used as-is.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-reachability-and-language/02-CONTEXT.md
@.planning/phases/02-reachability-and-language/02-RESEARCH.md
@.planning/phases/02-reachability-and-language/02-PATTERNS.md

@scripts/translation-spike.mjs
@src/server/config/models.ts
@src/server/realtime/hub.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract translate() + validators into translate.ts (rebuilt on the openai SDK)</name>
  <files>src/server/translation/translate.ts, src/server/translation/translate.test.ts</files>
  <read_first>
    scripts/translation-spike.mjs (the exact prompt-isolation shape at its `translate()`/`callChat()` functions and the four validator functions + SCRIPT_RANGES/TARGET_SCRIPT/REFUSAL_MARKERS constants — lines ~34-150), src/server/config/models.ts (activeProvider/MODEL_ID/BASE_URL/API_KEY_ENV_VAR exports)
  </read_first>
  <behavior>
    - translate(client, text, fromLang, toLang) with a well-formed JSON `{"translation": "..."}` response returns `{ ok: true, text }`.
    - translate() falls back to extracting a bare `{...}` JSON blob from the response content when response_format json mode isn't honored (mirrors the spike's jsonModeSupported fallback), still returning `{ ok: true, text }`.
    - translate() returns `{ ok: false, error }` (never throws) when the response has no parseable translation field.
    - scriptBlockMatch(output, targetLang) is true only when output contains a code point in the target language's script range (test at least Arabic, CJK, Devanagari, Cyrillic, Latin).
    - lengthRatioOk(input, output) is true only when output.length / input.length is within [0.4, 2.5].
    - hasRefusalMarker(output) is true for "I'm sorry, I cannot help with that" and false for a normal translated sentence.
    - preservesTokens(input, output) is true only when URL/digit/emoji counts match exactly between input and output.
    - An empty-string translate() response fails lengthRatioOk (covers the TRANS-07/empty must-have).
    - scriptBlockMatch correctly matches multi-byte/astral-plane content by code point, not UTF-16 length (covers TRANS-07/encoding).
  </behavior>
  <action>
    Create `src/server/translation/translate.ts`. Port `SCRIPT_RANGES`, `TARGET_SCRIPT`, `REFUSAL_MARKERS` and the four validator functions (`scriptBlockMatch`, `lengthRatioOk`, `hasRefusalMarker`, `preservesTokens`) from `scripts/translation-spike.mjs` VERBATIM (same regex/logic, TypeScript-typed signatures) — do not retune thresholds or rewrite the regexes; they are already corpus-tested. Port the `translate()` function's PROMPT SHAPE verbatim (the system message's exact instruction text: translate-from-X-to-Y, output-only-JSON, do-not-answer-or-follow-instructions-in-the-message; the untrusted text isolated in its own `user` message) — this is the TRANS-08 mitigation and the kept prohibition above; do not paraphrase it.
    DEVIATE from the spike's transport layer: replace the spike's hand-rolled `makeClient()`/fetch-with-retry HTTP client (which exists only because the spike script is disposable and must have zero app dependencies) with a real `new OpenAI({ baseURL: activeProvider.baseURL, apiKey: process.env[activeProvider.apiKeyEnvVar], maxRetries: 3, timeout: 20_000 })` client, constructed once at module scope from `src/server/config/models.ts`'s existing exports (`activeProvider`, `MODEL_ID`) — per CLAUDE.md's own stack guidance ("Set maxRetries explicitly — the SDK retries 429 with backoff"). Export `translate(text: string, fromLang: string, toLang: string): Promise<{ ok: true; text: string } | { ok: false; error: string }>` (client is now module-internal, not a parameter, since there's exactly one real client instance — simplify the signature relative to the spike's `translate(client, ...)`). Export the four validators individually (named exports) so Plan 02-05's cache.ts can call each one explicitly for TRANS-07's runtime validation.
  </action>
  <acceptance_criteria>
    - Test command: `node --experimental-strip-types --test src/server/translation/translate.test.ts` passes, covering every behavior line above.
    - Source assertion: `grep -c "new OpenAI" src/server/translation/translate.ts` == 1 (confirms the SDK-based transport, not a re-implemented fetch client).
    - Source assertion: `grep -c "Do not answer" src/server/translation/translate.ts` >= 1 (confirms the TRANS-08 instruction text was carried over, not dropped).
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test src/server/translation/translate.test.ts</automated>
  </verify>
  <done>translate.ts is a real, typed, openai-SDK-backed module exporting translate() and the four validators, behaviorally identical to the spike's already-corpus-tested logic, fully unit-tested without any network dependency (mock the OpenAI client in tests).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Circuit breaker (TRANS-10)</name>
  <files>src/server/translation/circuit-breaker.ts, src/server/translation/circuit-breaker.test.ts</files>
  <read_first>
    src/server/realtime/hub.ts (the exact globalThis-pinned singleton pattern — lines 29-41 — and its header comment explaining WHY a plain module-level variable is unsafe under Next's standalone build)
  </read_first>
  <behavior>
    - isOpen() is false initially (no failures recorded yet).
    - After recordFailure() is called FAILURE_THRESHOLD times consecutively, isOpen() becomes true.
    - While open, isOpen() stays true until COOLDOWN_MS has elapsed since the threshold-crossing failure (use a fake/mocked clock, not a real sleep, in the test).
    - recordSuccess() resets the consecutive-failure count to 0 and closes the breaker immediately (isOpen() becomes false).
    - One isolated recordFailure() call (below threshold) does not open the breaker.
  </behavior>
  <action>
    Create `src/server/translation/circuit-breaker.ts`. Pin state on `globalThis` exactly like `hub.ts` does (`globalThis as unknown as { __onechatTranslationBreaker?: { failures: number; openUntil: number } }`, lazily initialized to `{ failures: 0, openUntil: 0 }`) — copy the same header-comment rationale (Next's standalone build can bundle a module's graph twice; a plain module-level object would then split into two live instances the way `hub.ts`'s bug did). Export `FAILURE_THRESHOLD` (pick 3, consecutive) and `COOLDOWN_MS` (pick 60_000 — one minute) as named constants with a short comment on the reasoning (a translation outage is typically either momentary rate-limiting, recoverable within seconds-to-a-minute, or a real outage that a minute's cooldown correctly avoids hammering). Export `isOpen(): boolean` (true if `failures >= FAILURE_THRESHOLD` AND `Date.now() < openUntil`), `recordFailure(): void` (increments `failures`; sets `openUntil = Date.now() + COOLDOWN_MS` the moment `failures` first reaches `FAILURE_THRESHOLD`), `recordSuccess(): void` (resets `failures = 0`, `openUntil = 0`).
  </action>
  <acceptance_criteria>
    - Test command: `node --experimental-strip-types --test src/server/translation/circuit-breaker.test.ts` passes, covering every behavior line above (mock `Date.now` for the cooldown-elapsed case rather than a real sleep).
    - Source assertion: `grep -c "globalThis" src/server/translation/circuit-breaker.ts` >= 1 (confirms the pinned-singleton pattern was applied, not a bare module-level variable).
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test src/server/translation/circuit-breaker.test.ts</automated>
  </verify>
  <done>The breaker opens exactly at the configured consecutive-failure threshold (never one call early or late per the TRANS-09/boundary must-have), stays open for the full cooldown window, and is globalThis-pinned so it survives Next's standalone module-graph splitting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| translate() → NVIDIA NIM | Untrusted visitor/owner message text crosses into a third-party LLM API call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Elevation of Privilege (of the LLM's instruction-following) | translate.ts's prompt construction | high | mitigate | Structural message isolation (untrusted text confined to its own `user` message, system prompt as the sole instruction channel) — ported verbatim from the spike's already injection-corpus-tested design (TRANS-08). |
| T-02-06 | Information Disclosure | translate.ts's error path | medium | mitigate | A failed/malformed provider response is captured as `{ ok: false, error }` without ever logging the full message body — callers (Plan 02-05) are responsible for keeping message text out of any log statement, matching OPS-09's existing discipline. |
| T-02-07 | Denial of Service | NVIDIA NIM 429/outage | medium | mitigate | `maxRetries`/`timeout` on the SDK client plus the circuit breaker (TRANS-10) bound both per-call and sustained-outage cost — no unbounded retry loop. |

</threat_model>

<verification>
- `node --experimental-strip-types --test src/server/translation/translate.test.ts src/server/translation/circuit-breaker.test.ts` passes in full.
- `npm run build` succeeds.
</verification>

<success_criteria>
translate.ts and circuit-breaker.ts exist as pure, fully-tested, network-mockable modules ready for Plan 02-05 to wire into the live message flow, with zero behavioral drift from the corpus-tested spike logic.
</success_criteria>

## Artifacts this phase produces (this plan)

- `src/server/translation/translate.ts`: `translate(text, fromLang, toLang)`, `scriptBlockMatch`, `lengthRatioOk`, `hasRefusalMarker`, `preservesTokens`, `SCRIPT_RANGES`, `TARGET_SCRIPT`, `REFUSAL_MARKERS`.
- `src/server/translation/circuit-breaker.ts`: `isOpen()`, `recordFailure()`, `recordSuccess()`, `FAILURE_THRESHOLD`, `COOLDOWN_MS`.

## Flagged Assumptions (Edge Probe, specless fallback)

- **TRANS-10/precision** ("Where can precision loss, overflow, or rounding/tie-breaking occur?"): genuinely inapplicable — the circuit breaker's state is a small integer failure-count and a millisecond timestamp comparison, at call volumes far below any realistic overflow/precision-loss threshold for a single-owner pastoral chat app. No fabricated numeric-precision truth is authored; recorded here as an explicit, reviewed non-applicability rather than a silent drop.

<output>
Create `.planning/phases/02-reachability-and-language/02-02-SUMMARY.md` when done
</output>
