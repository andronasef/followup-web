---
phase: 02-reachability-and-language
plan: 08
type: execute
wave: 4
depends_on: ["02-01", "02-03", "02-04", "02-05", "02-06"]
files_modified:
  - src/lib/chat/useChatStream.ts
  - src/components/chat/ChatShell.tsx
  - src/app/page.tsx
  - src/components/chat/MessageBubble.tsx
  - src/components/admin/ReplyBox.tsx
  - src/components/admin/Thread.tsx
  - src/app/admin/(auth)/c/[id]/page.tsx
  - src/server/repo/conversations.ts
  - src/components/admin/ConversationRow.tsx
  - src/components/admin/GateFunnelStats.tsx
  - src/app/admin/(auth)/page.tsx
autonomous: true
requirements: [TRANS-04, ADMIN-09, OPS-11, PUSH-08]

must_haves:
  truths:
    - "If a message's translated rendering and its stored original happen to be textually identical (e.g. a short phrase that translates to itself), the 'See original'/'translated-primary' toggle still renders correctly wherever a translation row genuinely exists — this degenerate case is never suppressed or specially merged, it just reveals text identical to what's already shown."
    - "A conversation with zero messages renders the owner's thread view with no translation-related error — a translation batch-lookup for zero message ids is a no-op, not a query failure."
    - "The translated-primary/original-toggle rendering (both the visitor's MessageBubble and the owner's Thread) reuses the existing dir='auto' + unicode-bidi:isolate text handling for both the translated and original strings, so script-mixing (e.g. a Latin URL inside an Arabic original) renders correctly in whichever state is shown."
    - "Adding translated-primary rendering to Thread.tsx never changes message ordering — messages continue to render in the same ascending-id order Phase 1 established."
    - "The gate-funnel stats row renders literal '0' for a platform with zero visitors yet (not a missing row, not a placeholder dash), and all counts are all-time totals with no date-range filter, recomputed on every dashboard load."
  artifacts:
    - src/components/admin/GateFunnelStats.tsx
  key_links:
    - "MessageBubble.tsx and Thread.tsx consume the `translation` field Plan 02-05 added to since()/sinceAll()'s return shape, flowing through page.tsx/ChatShell.tsx/useChatStream.ts (visitor side) and admin c/[id]/page.tsx (owner side)."
    - "ReplyBox.tsx calls Plan 02-05's /api/admin/messages/translate-preview endpoint and posts originalBody to /api/admin/messages per that plan's reply.ts extension."
    - "useChatStream.ts's new ack-on-receipt call closes the loop Plan 02-04/02-06's send.ts grace-period check depends on (PUSH-08)."
    - "GateFunnelStats.tsx and ConversationRow.tsx's unreachable badge consume Plan 02-01's gateFunnel.statsByPlatform() and conversations.ts's extended listWithPreview() (this plan)."
---

<objective>
Build the user-visible half of the translation vertical — the visitor's "See original" toggle on owner replies, the owner's translated-primary Thread rendering with its own reveal-original toggle, the owner's composer draft-preview/edit/send flow — and the admin list's gate-funnel stats row and per-conversation unreachable badge.

Purpose: this is the final vertical-slice task that makes "the owner reads Swahili in English, replies in English shown as Swahili, and either side can always see the human's actual words" real for a person looking at the actual screen, completing the translation vertical Plan 02-02/02-05 built the data/API layer for. It also closes OPS-11's owner-facing visibility requirement.
Output: updated MessageBubble/ReplyBox/Thread/ConversationRow/admin pages, and a new GateFunnelStats component.

Per **D-09**, ReplyBox.tsx's Task 2 implements the inline-swap composer (the same textbox carries draft, then translated, text) rather than a side-by-side layout. Per **D-18**, ConversationRow.tsx's Task 3 renders the unreachable badge inline on the existing flat conversation-list row, never as a separate filtered section or new screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-reachability-and-language/02-CONTEXT.md
@.planning/phases/02-reachability-and-language/02-UI-SPEC.md
@.planning/phases/02-reachability-and-language/02-PATTERNS.md

@src/components/chat/MessageBubble.tsx
@src/lib/chat/useChatStream.ts
@src/components/chat/ChatShell.tsx
@src/app/page.tsx
@src/components/admin/ReplyBox.tsx
@src/components/admin/Thread.tsx
@src/app/admin/(auth)/c/[id]/page.tsx
@src/components/admin/ConversationRow.tsx
@src/app/admin/(auth)/page.tsx
@src/server/repo/conversations.ts
@src/lib/i18n/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visitor "See original" — MessageBubble/useChatStream/ChatShell/page.tsx + ACK-on-receipt</name>
  <files>src/components/chat/MessageBubble.tsx, src/lib/chat/useChatStream.ts, src/components/chat/ChatShell.tsx, src/app/page.tsx</files>
  <read_first>
    src/components/chat/MessageBubble.tsx (the exact bidi-isolation/dir='auto' text rendering to replicate for the revealed-original text), src/lib/chat/useChatStream.ts (ChatStreamMessage type, the "message" SSE listener), src/components/chat/ChatShell.tsx (the messageListItems useMemo mapping), src/app/page.tsx (initialMessages mapping)
  </read_first>
  <action>
    In `src/lib/chat/useChatStream.ts`: add `translation: string | null` to the `ChatStreamMessage` interface. In the existing "message" event listener, after appending a new row to `messages` state, if `row.sender === "owner"` fire a fire-and-forget `fetch("/api/chat/messages/ack", {method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify({messageId: row.id}), keepalive:true})` (matches `pre-paint.ts`'s existing fire-and-forget fetch style) — this is the client half of PUSH-08's grace-period ACK the SSE receipt itself satisfies; do not await it, do not block message rendering on it, and swallow any error silently (a failed ack just means `send.ts`'s grace-period check will (harmlessly) still fire a push).
    In `src/components/chat/ChatShell.tsx`'s `messageListItems` useMemo mapping, add `translation: message.translation` to the mapped object.
    In `src/app/page.tsx`'s `initialMessages` mapping, add `translation: message.translation` (from the now-extended `since()` return, Plan 02-05).
    In `src/components/chat/MessageBubble.tsx`: extend `ChatMessageLike` with an optional `translation?: string | null`. For a message where `sender === "owner"` AND `translation` is present and non-empty AND differs from `body` (TRANS-06's same-language-pair skip — no dead toggle otherwise): render a small tap-to-expand `showOriginalLabel`/`hideOriginalLabel` link (Label role, muted-foreground, per UI-SPEC's D-12 "familiar, low-weight affordance" — NOT the accent color) below the bubble's existing timestamp row; expanding it reveals `translation`'s text using the SAME `dir="auto"` + `unicodeBidi: "isolate"` treatment the primary body text already gets (reuse the identical `<p>` styling, do not write a second, divergent text-rendering block). For a visitor's own sent messages, or an owner message with no distinct translation, render nothing extra (unchanged from Phase 1).
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "unicodeBidi" src/components/chat/MessageBubble.tsx` >= 2 (confirms the revealed-original text reuses the same bidi-isolation treatment as the primary text, not a divergent implementation).
    - Source assertion: `grep -c "messages/ack" src/lib/chat/useChatStream.ts` >= 1.
    - Behavior assertion: an owner-sent message whose `translation` is null or equals `body` renders with NO "See original" toggle in the DOM.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>A visitor sees the delivered (translated) text by default on any owner reply, can reveal the owner's original-language draft when one was captured, never sees a dead toggle on a same-language message, and the live SSE receipt silently acks the message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Owner composer draft-preview (ReplyBox.tsx)</name>
  <files>src/components/admin/ReplyBox.tsx, src/components/admin/ReplyBox.test.ts</files>
  <read_first>
    src/components/admin/ReplyBox.tsx (the FULL existing file — its bounded-retry postReply pattern, the never-clear-on-failure discipline), 02-UI-SPEC.md's Copywriting Contract (composer-local English strings) and Color section (item 5 — Preview/Send button reuse) and UI Considerations (translation-preview loading/error rows)
  </read_first>
  <behavior>
    - The composer starts in 'draft' mode; typing and clicking the button in draft mode with no prior preview sends the typed text directly as `body` with no `originalBody` (unchanged Phase 1 behavior for a same-language or no-preview send).
    - Clicking "Preview" calls the translate-preview endpoint and, on success, switches to 'preview' mode showing the translated (editable) text, retaining the original draft text in state.
    - Editing the previewed text in 'preview' mode updates only the previewed text, never the retained original draft.
    - Clicking "Edit original" in 'preview' mode discards the preview and returns to 'draft' mode pre-loaded with the ORIGINAL draft text (not the edited preview) — per UI-SPEC's explicit "never silently merges an edited translation back onto the stored original" rule.
    - Sending from 'preview' mode posts `{conversationId, body: <current previewed/edited text>, clientMsgId, originalBody: <the retained original draft>}`.
    - A translate-preview failure/timeout shows the "Couldn't translate. You can still send your original message." notice with a "Send anyway" action that posts the draft text directly (no `originalBody` field, since there is no distinct translation to preserve).
  </behavior>
  <action>
    Extend `ReplyBox.tsx`'s state: add `mode: "draft" | "preview"`, `draftText: string` (renamed from the existing `text` state, or kept alongside it — preserve the existing never-clear-on-failure discipline for whichever field is currently being sent), `previewText: string`, `previewFailed: boolean`. Add a "Preview" button (aria-label "Preview translation" per UI-SPEC) alongside the existing Send button when in 'draft' mode with non-empty text; clicking it POSTs `{conversationId, draftText}` to `/api/admin/messages/translate-preview` (Plan 02-05), shows the bounded "Translating…" Label text in place of the button label while in flight (never an indefinite spinner, matching the visitor-side composer's existing pattern), and on success sets `mode:"preview"`, `previewText: result.translatedText`. On failure/`failed:true`, sets `previewFailed:true` and shows "Couldn't translate. You can still send your original message." with a "Send anyway" button that sends `draftText` directly (bypassing preview entirely). In 'preview' mode, the textarea is bound to `previewText` (editable, per D-10); an `Undo2`-icon "Edit original" button (aria-label "Edit your original message", mirrors in RTL per UI-SPEC's icon table) discards `previewText`/`previewFailed` and returns to `mode:"draft"` with `draftText` UNCHANGED in the textarea. The Send button (same button, relabeled per UI-SPEC's Color item 5 — no new button element) posts `body: mode === "preview" ? previewText : draftText` and, only when `mode === "preview"`, `originalBody: draftText`. On confirmed success, clear BOTH `draftText` and `previewText` and reset `mode` to `"draft"` (mirrors the existing never-clear-on-failure discipline — only a confirmed 200 clears anything).
  </action>
  <acceptance_criteria>
    - Test command: a node:test file exercising the composer's pure state-transition logic (extract a `reply-composer-logic.ts` sibling module if the JSX itself isn't type-strippable, matching `composer-logic.ts`'s established split from Plan 01-10) passes, covering every behavior line above.
    - Source assertion: `grep -c "originalBody" src/components/admin/ReplyBox.tsx` >= 1, present only on the preview-mode send path (region-scoped: verify by reading the conditional, not just a raw count).
    - Source assertion: `grep -c "Undo2" src/components/admin/ReplyBox.tsx` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test src/components/admin/reply-composer-logic.test.ts</automated>
  </verify>
  <done>The owner can draft, preview, edit, and send with the original preserved exactly when a distinct one exists; a translation failure never blocks sending; "Edit original" never silently merges an edit back onto the stored draft.</done>
</task>

<task type="auto">
  <name>Task 3: Owner Thread "See original" + admin list stats/badge</name>
  <files>src/components/admin/Thread.tsx, src/app/admin/(auth)/c/[id]/page.tsx, src/server/repo/conversations.ts, src/components/admin/ConversationRow.tsx, src/components/admin/GateFunnelStats.tsx, src/app/admin/(auth)/page.tsx</files>
  <read_first>
    src/components/admin/Thread.tsx (the full file — ThreadMessage type, SSE listener, initial render), src/app/admin/(auth)/c/[id]/page.tsx (the since() call + sender cast), src/server/repo/conversations.ts (listWithPreview's raw-sql LEFT JOIN LATERAL query), src/components/admin/ConversationRow.tsx (the exact row layout to extend), src/app/admin/(auth)/page.tsx (the guard+fetch+render shape)
  </read_first>
  <action>
    In `src/components/admin/Thread.tsx`: extend `ThreadMessage`/`StreamMessagePayload` with `translation: string | null`. For EVERY message (both senders — ADMIN-09 applies uniformly), render `translation ?? body` as the primary bubble text via `MessageBubble` (reuse the component, do not fork it); when `translation` is present and differs from `body`, render the same "See original"/"Hide original" toggle pattern as Task 1 (Label-role, muted-foreground), revealing `body` when expanded. A conversation with zero messages renders the existing empty list with no additional query/error (the translation data is already embedded per-message from Task 1's `since()` extension — there is no separate batch call to guard here, since Plan 02-05 already made this a no-extra-query join).
    In `src/app/admin/(auth)/c/[id]/page.tsx`: add `translation: row.translation` to the existing `messages` mapping (alongside the existing `sender` cast).
    In `src/server/repo/conversations.ts`'s `listWithPreview()`: extend the existing raw-sql query to also LEFT JOIN a per-conversation "is this visitor still reachable" check — add `unreachable: boolean` to `ConversationPreview`, computed as `(gate_funnel.granted_at is not null and not exists (select 1 from push_subscriptions where visitor_id = c.visitor_id))` per RESEARCH.md's Architecture Pattern 5 (a visitor who was once granted/subscribed but now has zero live subscriptions — distinct from "never granted push" at all, which is NOT unreachable, just never-subscribed).
    In `src/components/admin/ConversationRow.tsx`: add an optional `unreachable?: boolean` prop; when true, render a small, quiet inline badge (Label role, muted-foreground, `BellOff` icon per UI-SPEC's icon table, no rtl mirror) between the preview text and the timestamp, with the aria-label "This visitor's push notifications are no longer working" and visible text "Unreachable" — purely informational, no action (D-19).
    Create `src/components/admin/GateFunnelStats.tsx`: a small stats row (English, admin-local copy, per UI-SPEC's Copywriting Contract — no locale keys) showing "Shown" / "Reached prompt" / "Granted" counts split by "iOS" / "Other", each number rendered via `formatDigits()` (ASCII digits, no thousands separators, per D-17's resolved discretion), rendering literal `0` for a platform with NO row in the stats result at all (the component's own job to fill in a missing platform as zero, per Plan 02-01's gateFunnel.ts code comment).
    In `src/app/admin/(auth)/page.tsx`: fetch `gateFunnel.statsByPlatform()` alongside the existing `listWithPreview()` call, render `<GateFunnelStats>` between the header and the conversation list, and pass each conversation's new `unreachable` flag through to `<ConversationRow>`.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "unreachable" src/server/repo/conversations.ts` >= 1; `grep -c "BellOff" src/components/admin/ConversationRow.tsx` >= 1; `grep -c "formatDigits" src/components/admin/GateFunnelStats.tsx` >= 1.
    - Behavior assertion: `GateFunnelStats` given a `statsByPlatform()` result containing only an `'other'` row (no `'ios'` row) still renders three "0" values under the "iOS" column, not a blank/dash/missing row.
    - Behavior assertion: a conversation whose visitor has a `granted_at` but zero `push_subscriptions` rows renders the unreachable badge; one with a live subscription does not.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ADMIN-09's translated-plus-original view applies uniformly to both message directions in Thread.tsx; the admin list surfaces OPS-11's gate-funnel stats (zero-state correct) and D-18's unreachable badge, with zero new admin screens per D-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Visitor client → /api/chat/messages/ack (fire-and-forget from useChatStream.ts) | Already covered by Plan 02-06's T-02-19; this plan is only a new CALL SITE, not a new endpoint. |
| Owner dashboard → gate-funnel/unreachable data | Aggregate/derived data only, no new write surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-25 | Information Disclosure | ConversationRow's unreachable badge | low | accept | The badge exposes only a boolean reachability state per conversation to the already-authenticated owner — no new visitor-identifying data is surfaced beyond what `listWithPreview()` already returns. |
| T-02-26 | Tampering | ReplyBox's client-side mode/preview state | low | accept | `originalBody` is owner-authored, owner-trusted content (same trust boundary as T-02-17 in Plan 02-05) — a manipulated client could only affect that owner's own "see original" display of their own sent message. |

</threat_model>

<verification>
- `npm run build` succeeds.
- `node --experimental-strip-types --test src/components/admin/reply-composer-logic.test.ts` passes.
- Manual check: a Swahili-language conversation's visitor message renders in English (OWNER_LANG) by default in Thread.tsx, with "See original" revealing the Swahili text; an owner reply previewed-and-edited renders as the edited text on the visitor's side, with "See original" revealing the owner's pre-edit draft.
</verification>

<success_criteria>
Both directions of TRANS-04/ADMIN-09's "translated plus original on demand" are real, on-screen, and correctly gated on same-language skip; OPS-11's stats row and D-18's unreachable badge are visible on the existing conversation-list screen with no new admin surface.
</success_criteria>

## Artifacts this phase produces (this plan)

- `src/components/admin/GateFunnelStats.tsx`: new.
- `src/lib/chat/useChatStream.ts` (additive): `translation` field, ack-on-receipt call.
- `src/components/chat/ChatShell.tsx`, `src/app/page.tsx` (additive): `translation` field passthrough.
- `src/components/chat/MessageBubble.tsx` (additive): "See original" toggle.
- `src/components/admin/ReplyBox.tsx` (additive): draft/preview state machine.
- `src/components/admin/Thread.tsx`, `src/app/admin/(auth)/c/[id]/page.tsx` (additive): translated-primary rendering + toggle.
- `src/server/repo/conversations.ts` (additive): `unreachable` in `ConversationPreview`.
- `src/components/admin/ConversationRow.tsx` (additive): unreachable badge.
- `src/app/admin/(auth)/page.tsx` (additive): stats row + badge wiring.

## Flagged Assumptions (Edge Probe, specless fallback)

None — every edge item routed here (ADMIN-09's four rows) resolved to a concrete, testable truth above.

<output>
Create `.planning/phases/02-reachability-and-language/02-08-SUMMARY.md` when done
</output>
