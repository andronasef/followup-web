---
phase: 01-foundation-and-the-realtime-spine
plan: 07
type: execute
wave: 3
depends_on: ["01-01", "01-04"]
files_modified:
  - src/app/api/admin/setup/route.ts
  - src/app/admin/setup/page.tsx
  - src/app/api/admin/login/route.ts
  - src/app/admin/login/page.tsx
  - src/server/auth/guard.ts
  - src/proxy.ts
autonomous: true
requirements: [ADMIN-01]
user_setup:
  - service: admin-setup-token
    why: "The one-time owner-account-creation route (D-14) additionally requires a setup token supplied as a runtime env var, so an open account-creation endpoint is never reachable by guesswork alone"
    env_vars:
      - name: SETUP_TOKEN
        source: "Generate a long random value yourself (e.g. openssl rand -hex 32) and set it as a runtime-only env var before first deploy; never commit it, never mark it build-time in Coolify"
must_haves:
  truths:
    - "Only the D-14 one-time setup flow can ever create the single responders row (owner account) — there is no other registration path, and login only succeeds against that one Argon2id-hashed credential."
    - "The setup route (page and POST handler) returns 404 the instant a responders row exists, checked server-side on every request, never cached — and this holds across a container restart and a fresh deploy against an existing DB, not just within one process lifetime."
    - "Admin login returns the identical error string for a wrong email and a wrong password — the form never enumerates whether an account exists."
    - "proxy.ts verifies the owner session cookie's typ==='owner' claim before allowing any request through to /admin/* (excluding /admin/setup and /admin/login themselves), and never trusts a visitor-typed cookie for admin access."
  artifacts:
    - path: "src/app/api/admin/setup/route.ts"
      provides: "POST — 404-by-construction owner account creation, gated by SETUP_TOKEN"
    - path: "src/server/auth/guard.ts"
      provides: "requireOwner() — verifies the owner session cookie, used by admin routes and pages"
    - path: "src/proxy.ts"
      provides: "Next 16 proxy (middleware rename) guarding /admin/* except /admin/setup and /admin/login"
  key_links:
    - from: "src/app/api/admin/setup/route.ts"
      to: "src/server/db/schema.ts"
      via: "select exists(select 1 from responders) — the existence check that makes the route self-disable"
      pattern: "exists"
    - from: "src/proxy.ts"
      to: "src/server/auth/session.ts"
      via: "verifySession, branching on typ === 'owner'"
      pattern: "typ"
  prohibitions:
    - requirement_id: "ADMIN-01"
      category: "safety"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT return different error copy, status codes, or timing-distinguishable behavior for 'wrong email' vs 'wrong password' on admin login — one identical string and response shape for both, so the login form never enumerates valid accounts."
    - requirement_id: "ADMIN-01"
      category: "safety"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT leave /admin/setup (page or POST handler) reachable once a responders row exists, including after a container restart or a fresh deploy against an existing DB — the existence check must run on every request, never be cached in memory or behind a build-time flag. This is the highest-risk item flagged in CONTEXT.md."
  assumptions: []
---

<objective>
Wire the owner side of authentication: a self-disabling one-time setup route (D-14's hard security constraint), an account-enumeration-safe login route, and `proxy.ts` guarding every `/admin/*` route. This plan consumes `session.ts` and `password.ts` from Plan 01-04 without modifying them.

Purpose: ADMIN-01's login flow and D-14's setup-route hard constraint are the single highest-risk surface flagged in this phase's CONTEXT.md — an open account-creation endpoint on a single-owner pastoral product. This must be closed by construction, not by discipline.
Output: `/admin/setup`, `/admin/login`, `guard.ts`, `proxy.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-time setup route — 404 by construction</name>
  <files>src/app/api/admin/setup/route.ts, src/app/admin/setup/page.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Pattern 5, lines ~434-452 — the exact 404-by-construction core; Security Domain's "Elevation of Privilege" row on this exact route)
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-14 in full — the three hard-constraint bullets: exists-check on every request never cached, additionally requires the env token, never reachable again after setup including after restart/redeploy against an existing DB)
    src/server/auth/password.ts (from Plan 01-04 — hashPassword)
  </read_first>
  <action>
    Create src/app/api/admin/setup/route.ts exporting POST: first statement is a fresh DB query select exists(select 1 from responders) — if true, return new Response(null, { status: 404 }) immediately, before touching the request body or the setup token at all. Only if no responders row exists, check the x-setup-token header against process.env.SETUP_TOKEN; a mismatch also returns 404 (not 403 — a 403 would confirm the route's existence to a prober; 404 is indistinguishable from a route that was never there). Only past both gates, parse the body with a zod schema { email, password, displayName }, hash the password via hashPassword, insert the one responders row, and return 200. Create src/app/admin/setup/page.tsx as a Server Component that runs the identical exists() check server-side (an RSC await, not a client-side redirect) and calls notFound() from next/navigation if a responders row already exists, so the page itself 404s — not just the POST endpoint. The page's form posts to the setup route with the setup token supplied via a query param or a manual paste field (documented in the SUMMARY as a one-time manual step the owner performs using the SETUP_TOKEN env var they configured).
  </action>
  <acceptance_criteria>
    The exists() check is the literal first database operation in the POST handler, before any header or body access — verified by reading the function body top-to-bottom.
    A request with a valid setup token succeeds exactly once; a second POST attempt (after a responders row now exists) returns 404 regardless of token correctness.
    src/app/admin/setup/page.tsx calls notFound() when a responders row exists, verified by the exists check appearing before any form-rendering JSX.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "exists" src/app/api/admin/setup/route.ts | head -1 | grep -qE ':[1-9][0-9]?:' && grep -c "SETUP_TOKEN" src/app/api/admin/setup/route.ts | grep -qE '^[1-9]' && grep -c "notFound" src/app/admin/setup/page.tsx | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>The setup route and page are both self-disabling by construction, checked fresh on every request, additionally gated by an env-supplied token, and return 404 (not a distinguishable error) once used.</done>
</task>

<task type="auto">
  <name>Task 2: Owner login — non-enumerating, session issuance</name>
  <files>src/app/api/admin/login/route.ts, src/app/admin/login/page.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Copywriting Contract — "Admin login must not enumerate. One string for wrong email and wrong password alike.")
    src/server/auth/session.ts (signOwnerSession)
    src/server/auth/password.ts (verifyPassword)
  </read_first>
  <action>
    Create src/app/api/admin/login/route.ts exporting POST: parse { email, password } via zod, look up the responders row by lowercased email. Whether the lookup fails (no such email) OR verifyPassword fails, take the same code path to a single generic 401 response with the exact locked copy ("That email or password isn't right.") — no branch that reveals which half was wrong, and constant-shape timing (e.g., still call verifyPassword against a dummy hash when the email lookup misses, so a timing side-channel cannot distinguish "no such email" from "wrong password" by response latency). On success, sign an owner session via signOwnerSession(responder.id) and set it as an HttpOnly, Secure, SameSite=Strict cookie (Strict, not Lax — RESEARCH.md's Security Domain explicitly recommends Strict for the admin cookie since there is no cross-site navigation requirement for an admin-only surface; the visitor cookie stays Lax per ID-01, this is a deliberate difference). Create src/app/admin/login/page.tsx with the email/password form per UI-SPEC.md's typography/spacing/color contract, posting to the login route and rendering the one generic error string on any failure.
  </action>
  <acceptance_criteria>
    The route's failure path is a single shared code branch/return for both "no such email" and "wrong password" — verified by there being exactly one 401 response construction in the handler, not two differently-worded ones.
    A successful login sets a cookie with SameSite=Strict (not Lax) for the admin session, distinct from the visitor cookie's Lax attribute.
    The rendered error message on the login page matches the exact locked copy from UI-SPEC.md.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "isn't right" src/app/api/admin/login/route.ts | grep -qE '^1$' && grep -ci "SameSite=Strict\|sameSite: 'strict'\|sameSite: \"strict\"" src/app/api/admin/login/route.ts | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>Login never enumerates accounts, issues a Strict-cookie owner session on success, and uses the exact locked error copy.</done>
</task>

<task type="auto">
  <name>Task 3: guard.ts and proxy.ts — owner-only route protection</name>
  <files>src/server/auth/guard.ts, src/proxy.ts</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Security Domain V4 Access Control row; Next.js 16 Migration Notes — proxy.ts rename, nodejs-only runtime, no edge)
  </read_first>
  <action>
    Create src/server/auth/guard.ts exporting requireOwner(): reads the admin session cookie, verifies via verifySession, throws/returns null if missing, invalid, or typ !== 'owner' — used by admin Route Handlers and Server Components that need the responder id inline. Create src/proxy.ts (Next 16's rename of middleware.ts — export a function named proxy, not middleware) matching /admin/:path* except /admin/setup and /admin/login, verifying the owner cookie via the same verifySession call and redirecting to /admin/login on failure; explicitly do not attempt export const runtime = 'edge' — Next 16's proxy.ts is nodejs-only and not configurable, per RESEARCH.md's migration notes.
  </action>
  <acceptance_criteria>
    src/proxy.ts exports a function literally named proxy (not middleware).
    The proxy's matcher excludes /admin/setup and /admin/login from the redirect-on-failure check (both must remain reachable without a valid session, since login/setup are how you obtain one).
    guard.ts's requireOwner rejects a payload where typ !== 'owner', even if the signature verifies correctly (i.e., a visitor cookie must not grant admin access).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export function proxy\|export const proxy" src/proxy.ts | grep -qE '^[1-9]' && grep -c "admin/setup" src/proxy.ts | grep -qE '^[1-9]' && grep -c "typ" src/server/auth/guard.ts | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>Every /admin/* route except setup and login is guarded; a visitor-typed cookie is explicitly rejected for admin access; proxy.ts uses the correct Next 16 export name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated -> /admin/setup | The single highest-risk boundary in this phase — must be closed by construction the instant one responder exists |
| Unauthenticated -> /admin/login | Public by necessity; must not leak account existence |
| /admin/* (all else) | Requires a verified owner-typed session cookie, enforced at the proxy layer before any route handler runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Elevation of Privilege | src/app/api/admin/setup/route.ts | high | mitigate | 404-by-construction exists() check as the literal first operation, plus a runtime-env SETUP_TOKEN — two independent gates, neither cached |
| T-01-20 | Spoofing | src/app/api/admin/login/route.ts | medium | mitigate | Single non-enumerating error path with constant-shape timing (dummy-hash verify on email-miss) |
| T-01-21 | Elevation of Privilege | src/proxy.ts | high | mitigate | Verifies typ==='owner' on every /admin/* request except the two unauthenticated entry points; a valid-but-wrong-typ cookie (a visitor's) is explicitly rejected |
| T-01-22 | Tampering | SETUP_TOKEN | high | mitigate | Runtime-only env var (never build-time, never committed) per this plan's user_setup — owner generates it themselves before first deploy |
</threat_model>

<verification>
Setup route/page 404 the instant a responder exists and stay 404 across restarts (structural — the check is DB-driven, not process-memory-driven, so this holds by construction); login never distinguishes wrong-email from wrong-password; proxy.ts rejects non-owner-typed cookies from /admin/*.
</verification>

<success_criteria>
ADMIN-01 is fully wired: the owner can be created exactly once, log in without account enumeration, and every admin route is protected — ready for Plan 01-11's admin UI to sit behind this guard.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `src/app/api/admin/setup/route.ts`, `src/app/admin/setup/page.tsx` — D-14 one-time setup
- `src/app/api/admin/login/route.ts`, `src/app/admin/login/page.tsx` — non-enumerating login
- `src/server/auth/guard.ts` — `requireOwner()`
- `src/proxy.ts` — `/admin/*` route protection

<output>
Create `.planning/phases/01-foundation-and-the-realtime-spine/01-07-SUMMARY.md` when done
</output>
