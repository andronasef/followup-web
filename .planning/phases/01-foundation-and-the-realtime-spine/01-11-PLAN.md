---
phase: 01-foundation-and-the-realtime-spine
plan: 11
type: execute
wave: 5
depends_on: ["01-07", "01-08"]
files_modified:
  - src/app/admin/(auth)/page.tsx
  - src/app/admin/(auth)/c/[id]/page.tsx
  - src/components/admin/ConversationRow.tsx
  - src/components/admin/Thread.tsx
  - src/components/admin/ReplyBox.tsx
autonomous: true
requirements: [ADMIN-03]
must_haves:
  truths:
    - "A logged-in owner can open any existing conversation from the flat list and their reply is persisted and delivered to the visitor through the exact same durability+realtime path a visitor's message uses (ADMIN-03) — not a separate, lesser code path."
    - "With zero conversations, the list renders the documented empty-state heading and body copy ('No conversations yet' / the exact locked body text) rather than a blank screen."
    - "The admin thread view updates live as new messages arrive on the admin SSE stream, without a page refresh, mirroring the visitor side's real-time behavior."
    - "The admin list preview truncates to a single line with logical-property ellipsis, correct at both the inline-start and inline-end under RTL, when the last message is long."
    - statement: "The list scale ceiling (many conversations, no virtualization) is unknown at Phase 1 and accepted as a plain-scroll assumption; this is explicitly deferred to Phase 3, not resolved here."
      verification: backstop
  artifacts:
    - path: "src/app/admin/(auth)/page.tsx"
      provides: "Flat conversation list (D-12), no filters/sort/status/faith-flag"
    - path: "src/app/admin/(auth)/c/[id]/page.tsx"
      provides: "Thread view + reply composer, wired to admin SSE"
  key_links:
    - from: "src/components/admin/ReplyBox.tsx"
      to: "src/app/api/admin/messages/route.ts"
      via: "POST reply, requireOwner-guarded (Plan 01-08)"
      pattern: "admin/messages"
    - from: "src/app/admin/(auth)/c/[id]/page.tsx"
      to: "src/app/api/admin/stream/route.ts"
      via: "EventSource against the owner-scoped firehose, filtered client-side to this conversation id"
      pattern: "admin/stream"
  prohibitions:
    - requirement_id: "ADMIN-03"
      category: "values"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT build inbox filters, priority sort, status controls, or a faith-decision flag in this Phase 1 admin surface — D-12 explicitly scopes this phase to a flat, unsorted conversation list plus a thread view only. That surface area (ADMIN-05 through ADMIN-11) is Phase 3's."
  assumptions:
    - "UI-SPEC.md's zero-one-many/admin-conversation-list row is explicitly unresolved — the many-conversation-count scale ceiling is unknown and accepted as an assumption for Phase 1 (plain scroll, no virtualization), deferred to Phase 3 re-evaluation."
---

<objective>
Build the minimal three-screen owner surface's remaining two screens (login/setup already built in Plan 01-07): a flat conversation list and a thread view with a reply composer, wired to the owner-scoped SSE stream and the durable reply route from Plan 01-08. Deliberately excludes every piece of Phase 3's real inbox (filters, sort, counts, status, faith flag) per D-12.

Purpose: ADMIN-03 — "the owner can read a conversation and send a reply" — is the piece that lets the realtime spine be exercised end to end from both sides, which is this whole phase's point.
Output: `/admin` (flat list), `/admin/c/[id]` (thread), reply composer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-07-SUMMARY.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flat conversation list</name>
  <files>src/app/admin/(auth)/page.tsx, src/components/admin/ConversationRow.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-12 — three screens, no filters/sort/status/faith-flag; Open Question 3 — flat scroll without virtualization accepted as a Phase 1 assumption)
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Copywriting Contract's empty-state heading/body; long-text/admin-list-preview backstop row; Spacing Scale's 2xl empty-state inset)
    src/server/auth/guard.ts (requireOwner, from Plan 01-07)
  </read_first>
  <action>
    Create src/app/admin/(auth)/page.tsx as a Server Component guarded by requireOwner() (redirecting to /admin/login if unauthenticated — proxy.ts from Plan 01-07 already enforces this at the edge, but the page itself should not assume it is unreachable otherwise). Query all conversations ordered by most-recent-message (a plain ORDER BY, no priority weighting — D-12 explicitly forbids building ADMIN-05's sort here) and render one ConversationRow per conversation, or the exact locked empty-state heading/body when the list is empty. Create src/components/admin/ConversationRow.tsx: a row showing the conversation's last-message preview (truncated to one line using CSS logical properties for the ellipsis so it is correct under both LTR and RTL — text-overflow with the inline-start/inline-end aware approach, not a hardcoded left/right truncation), the last-message time via the shared ASCII-digit formatter, and links to /admin/c/[id]. No status badge, no faith-decision flag, no filter chips anywhere on this screen.
  </action>
  <acceptance_criteria>
    src/app/admin/(auth)/page.tsx contains no filter/status/faith-flag UI element and no ORDER BY clause referencing a priority/faith-decision column — plain recency ordering only.
    The empty-state renders the exact locked heading and body text when zero conversations exist.
    ConversationRow.tsx uses a logical-property truncation approach (e.g. Tailwind's truncate utility combined with text-start, not a hardcoded text-align: left).
  </acceptance_criteria>
  <verify>
    <automated>! grep -iE "faith|priority|status.*filter" "src/app/admin/(auth)/page.tsx" && grep -c "No conversations yet" "src/app/admin/(auth)/page.tsx" | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>The list is flat, unfiltered, unsorted-beyond-recency, shows the correct empty state, and previews truncate correctly under both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Thread view</name>
  <files>src/app/admin/(auth)/c/[id]/page.tsx, src/components/admin/Thread.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Architectural Responsibility Map — owner auth server-only; D-13's owner-scope-over-the-same-hub note)
    src/components/chat/MessageBubble.tsx (from Plan 01-10 — reuse this component for owner-view message rendering rather than duplicating bidi/delivery-state logic)
  </read_first>
  <action>
    Create src/app/admin/(auth)/c/[id]/page.tsx: fetches the conversation's message history server-side (initial SSR render, using the same repo.messages.since(id, 0) query Plan 01-08 exposes) and renders it via src/components/admin/Thread.tsx, which reuses src/components/chat/MessageBubble.tsx for individual message rendering (do not duplicate the bidi-isolation/delivery-state logic — import and reuse it) so the owner sees the same correctly-isolated text a visitor would. No translation UI exists yet (ADMIN-09 is Phase 2) — messages render as their original stored text only. Include a ChevronLeft back-affordance to the list (the one other allowlisted mirroring icon relevant to the admin surface).
  </action>
  <acceptance_criteria>
    Thread.tsx imports MessageBubble from src/components/chat/MessageBubble.tsx rather than reimplementing bidi isolation.
    The thread page's initial render includes the conversation's message history without waiting for the SSE connection to open (SSR data, not client-only fetch-on-mount).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "MessageBubble" src/components/admin/Thread.tsx | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>The thread view reuses the visitor-side message rendering, shows original text only (no translation UI this phase), and SSR-loads history.</done>
</task>

<task type="auto">
  <name>Task 3: Reply composer + admin SSE wiring</name>
  <files>src/components/admin/ReplyBox.tsx</files>
  <read_first>
    src/app/api/admin/messages/route.ts, src/app/api/admin/stream/route.ts (from Plan 01-08 — the reply POST contract and the owner-scoped firehose)
  </read_first>
  <action>
    Create src/components/admin/ReplyBox.tsx: a composer posting to /api/admin/messages with the target conversationId, reusing the same optimistic-send-with-client_msg_id pattern from Plan 01-10's Composer.tsx conceptually (a lighter version is fine — this surface does not need D-19's full crisis-typing retry ceremony, but must still not lose the owner's typed reply on a transient failure). Wire the thread page to /api/admin/stream via EventSource, filtering incoming events client-side to the current conversationId (the admin stream is a firehose per D-13 — every conversation's events arrive on one connection, and this component's job is to render only the ones matching the open thread) and appending new messages to the rendered list live.
  </action>
  <acceptance_criteria>
    ReplyBox.tsx's POST call targets /api/admin/messages, not /api/chat/messages.
    The thread's EventSource consumer filters incoming events by conversationId before appending to the rendered list (does not render every conversation's messages into one thread).
    A failed reply POST does not clear the ReplyBox's text field.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "admin/messages" src/components/admin/ReplyBox.tsx | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>Owner replies durably persist via the same-pattern write path, and the thread updates live filtered from the owner-scoped firehose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/(auth)/* pages | Guarded by proxy.ts (Plan 01-07) at the edge; pages themselves also assume requireOwner() may be called defensively, never assuming the guard alone is sufficient |
| Admin SSE firehose -> Thread filtering | The firehose itself is authorization-scoped (owner-only, per Plan 01-08); per-conversation filtering here is a rendering concern, not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-32 | Elevation of Privilege | src/app/admin/(auth)/c/[id]/page.tsx | high | mitigate | Server-side conversation fetch runs only after requireOwner() succeeds (enforced both at proxy.ts and defensively in the page); no conversationId-based visitor-side authorization bypass exists since owner scope is global by design (D-13) |
| T-01-33 | Tampering (XSS) | src/components/admin/Thread.tsx | high | mitigate | Reuses MessageBubble.tsx's plain-text JSX rendering — no dangerouslySetInnerHTML introduced on the admin side either |
</threat_model>

<verification>
The list is flat/unfiltered with a correct empty state; the thread reuses visitor-side bidi/delivery rendering; replies persist via the same durability pattern and appear live via the filtered admin SSE stream.
</verification>

<success_criteria>
ADMIN-03 is fully satisfied: the owner can read any conversation and reply, exercised end to end against the same realtime spine the visitor side uses, with zero Phase 3 surface area leaking in.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `src/app/admin/(auth)/page.tsx` — flat conversation list
- `src/app/admin/(auth)/c/[id]/page.tsx` — thread view
- `src/components/admin/ConversationRow.tsx`, `Thread.tsx`, `ReplyBox.tsx`

<output>
Create `.planning/phases/01-foundation-and-the-realtime-spine/01-11-SUMMARY.md` when done
</output>
