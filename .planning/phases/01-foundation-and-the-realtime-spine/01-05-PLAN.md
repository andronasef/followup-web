---
phase: 01-foundation-and-the-realtime-spine
plan: 05
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/server/i18n/detect.ts
  - src/server/i18n/dir.ts
  - src/lib/i18n/format.ts
  - src/lib/i18n/locales/en.json
  - src/lib/i18n/locales/ar.json
  - src/lib/i18n/locales/es.json
  - src/lib/i18n/locales/fr.json
  - src/lib/i18n/locales/pt.json
  - src/lib/i18n/locales/hi.json
  - src/lib/i18n/locales/zh.json
  - src/lib/i18n/locales/ru.json
  - src/lib/i18n/locales/id.json
  - src/lib/i18n/locales/sw.json
autonomous: true
requirements: [LANG-01, LANG-07]
must_haves:
  truths:
    - "On first load with no cookie and no localStorage, the interface language is derived from the browser's Accept-Language/navigator.language, matched against the confirmed supported list, before any UI paints (LANG-01)."
    - "An unsupported browser locale falls back directly to English (D-10) — there is no language-family mapping table anywhere in detect.ts."
    - "Every locale JSON file for the confirmed language list contains a value for every key used by the UI-SPEC Copywriting Contract — no key is missing in any file, and no component falls through to a hardcoded English literal."
    - "dir.ts maps each supported language code to 'rtl' or 'ltr' with Arabic as the only rtl entry in the confirmed list (no other candidate language in the ten/nine-language list is RTL)."
  artifacts:
    - path: "src/server/i18n/detect.ts"
      provides: "detectLanguage(acceptLanguageHeader, supportedList) -> language code, English fallback, no mapping table"
    - path: "src/server/i18n/dir.ts"
      provides: "dirFor(languageCode) -> 'ltr' | 'rtl'"
    - path: "src/lib/i18n/locales/*.json"
      provides: "Complete localized string sets for the confirmed language list (9 or 10, per Plan 01-02's go/no-go)"
  key_links:
    - from: "src/lib/i18n/locales/*.json"
      to: ".planning/phases/01-foundation-and-the-realtime-spine/TRANSLATION-SPIKE-GO-NO-GO.md"
      via: "the set of locale files authored here matches exactly the language list the go/no-go decided (Swahili included only if the spike passed it)"
      pattern: "go/no-go"
  prohibitions:
    - requirement_id: "LANG-01"
      category: "values"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT use a language-family mapping table to guess a fallback locale from an unsupported browser locale — fall back directly to English with the language control visibly nudged on first load (D-10 explicit prohibition); the visible-nudge half is implemented in Plan 01-09's Header/LanguageSheet, not here."
  assumptions:
    - "LANG-07 'adjacency' and 'ordering' edges (from 01-EDGE-PROBE.json) do not meaningfully apply — localization completeness is a per-key coverage constraint, not a merge/collide or order-dependent operation. Flagged, not forced into truths."
---

<objective>
Build the i18n detection/direction mechanism and author the full locale JSON set for the language list confirmed by Plan 01-02's go/no-go. This plan cannot start authoring locale content until the spike decision (nine vs ten languages, specifically whether Swahili ships) is known — that dependency is explicit in `depends_on`.

Purpose: Settle LANG-01 (auto-detect) and LANG-07 (full localization) mechanically, so every later UI plan (01-09, 01-10, 01-11) consumes locale keys instead of hardcoding strings.
Output: `server/i18n/detect.ts`, `server/i18n/dir.ts`, `lib/i18n/format.ts`, and one JSON file per confirmed language.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md
@.planning/phases/01-foundation-and-the-realtime-spine/TRANSLATION-SPIKE-GO-NO-GO.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Language detection, direction lookup, digit/format helper</name>
  <files>src/server/i18n/detect.ts, src/server/i18n/dir.ts, src/lib/i18n/format.ts</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-10 — no language-family mapping table, visible nudge)
    .planning/phases/01-foundation-and-the-realtime-spine/TRANSLATION-SPIKE-GO-NO-GO.md (the confirmed language list this plan targets)
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md ("Don't Hand-Roll" table — digit/number formatting consistency row, one shared ASCII-digits-everywhere helper)
  </read_first>
  <behavior>
    - Test: detectLanguage('ar-SA,ar;q=0.9', SUPPORTED) returns 'ar' when 'ar' is in the supported list.
    - Test: detectLanguage('de-DE,de;q=0.9', SUPPORTED) returns 'en' (German is not in the confirmed list, no family-mapping guess toward a related language).
    - Test: detectLanguage(undefined, SUPPORTED) (no header at all) returns 'en'.
    - Test: dirFor('ar') returns 'rtl'; dirFor('en') and every other confirmed language return 'ltr'.
    - Test: formatDigits(1234) or the exported ASCII-digit helper returns plain ASCII digits regardless of the active locale (never Arabic-Indic digits).
  </behavior>
  <action>
    Create src/server/i18n/detect.ts exporting SUPPORTED_LANGUAGES (the exact confirmed list from TRANSLATION-SPIKE-GO-NO-GO.md — read that file's decision before hardcoding the array) and detectLanguage(acceptLanguageHeader, supported = SUPPORTED_LANGUAGES): parse the header's language tags in quality order, return the first tag (base subtag only, e.g. 'ar' from 'ar-SA') present in supported; if none match or the header is absent, return 'en'. Do not build any table mapping unsupported locales to "closest" supported ones — this is D-10's explicit prohibition. Create src/server/i18n/dir.ts exporting dirFor(languageCode): 'rtl' for 'ar', 'ltr' for every other code in SUPPORTED_LANGUAGES. Create src/lib/i18n/format.ts exporting a shared digit/number formatting helper that always renders ASCII digits (0-9), documented as the single point of truth every component must use instead of ad-hoc toLocaleString() calls, per RESEARCH.md's Don't-Hand-Roll guidance.
  </action>
  <acceptance_criteria>
    All five behavior tests pass.
    detect.ts contains no data structure resembling a language-family/similarity mapping (no object mapping e.g. 'de' -> 'en' or grouping by script family beyond the literal SUPPORTED_LANGUAGES membership check).
    dir.ts returns 'rtl' for exactly one language code in the confirmed list.
  </acceptance_criteria>
  <verify>
    <automated>node --test src/server/i18n/*.test.* 2>&1 | tail -30 || npm test -- --testPathPattern=i18n 2>&1 | tail -30</automated>
  </verify>
  <done>Detection falls back to English with no mapping table, direction lookup is correct for the confirmed list, and a shared ASCII-digit formatter exists.</done>
</task>

<task type="auto">
  <name>Task 2: Author locale JSON for the confirmed language list</name>
  <files>src/lib/i18n/locales/en.json, src/lib/i18n/locales/ar.json, src/lib/i18n/locales/es.json, src/lib/i18n/locales/fr.json, src/lib/i18n/locales/pt.json, src/lib/i18n/locales/hi.json, src/lib/i18n/locales/zh.json, src/lib/i18n/locales/ru.json, src/lib/i18n/locales/id.json, src/lib/i18n/locales/sw.json</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Copywriting Contract table in full — every row is a required key)
    .planning/phases/01-foundation-and-the-realtime-spine/TRANSLATION-SPIKE-GO-NO-GO.md (confirms whether sw.json should be authored at all — if Swahili was dropped, do not create src/lib/i18n/locales/sw.json and remove it from this plan's files_modified accordingly, and note the nine-language list explicitly in the SUMMARY)
  </read_first>
  <action>
    Author src/lib/i18n/locales/en.json first as the authoritative source-language key set, using the exact English copy from UI-SPEC.md's Copywriting Contract table: sendAriaLabel, changeLanguageAriaLabel, languageSheetTitle, appearanceAriaLabelDark, appearanceAriaLabelLight, composerPlaceholder, welcomeLine1, welcomeLine2Online, welcomeLine2Offline, emptyStateHeading, emptyStateBody, errorSendFailed, errorRateLimited, errorReconnectStuck, errorAdminLogin, adminCtaLogin, adminCtaSetup — plus any additional structural keys detect.ts/dir.ts need (e.g., an endonym display name key per language, used by Plan 01-09's language sheet: العربية for ar, 中文 for zh, Kiswahili for sw, etc., per D-09). Then author one JSON file per remaining confirmed language with the identical key set, each value translated into that language, preserving the exact meaning and tone locked in UI-SPEC.md (two-line welcome, no preamble, gentle non-cutoff rate-limit copy, non-enumerating login error). If OVH access is available at this point, use the same translation infrastructure pattern as the spike (not a live API call from this task — hand-author or machine-assist offline, since Phase 1 has zero translation calls in the running app per the Architectural Responsibility Map). Verify key-set parity across all files before finishing.
  </action>
  <acceptance_criteria>
    Every locale JSON file has an identical set of top-level keys (verified by diffing key lists across all files, not just en.json's presence).
    No locale file contains an empty string value for any Copywriting Contract key.
    The set of locale files present matches exactly the language list decided in TRANSLATION-SPIKE-GO-NO-GO.md (9 files if Swahili was dropped, 10 if it shipped).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=fs.readdirSync('src/lib/i18n/locales').filter(f=>f.endsWith('.json')); const keysets=files.map(f=>JSON.stringify(Object.keys(JSON.parse(fs.readFileSync('src/lib/i18n/locales/'+f,'utf8'))).sort())); const allSame=keysets.every(k=>k===keysets[0]); if(!allSame){console.error('key mismatch across locale files');process.exit(1)} console.log('ok', files.length, 'locales')"</automated>
  </verify>
  <done>All locale files exist with identical, complete key sets matching UI-SPEC.md's Copywriting Contract, for exactly the confirmed language list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accept-Language header -> detectLanguage | Client-controlled input; only ever used to select a key into a fixed enum-like list, never interpolated into a query, path, or template |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Tampering | src/server/i18n/detect.ts | low | mitigate | The header value only ever indexes into SUPPORTED_LANGUAGES; an unrecognized or malicious value falls through to the 'en' default, never used to construct a file path or query |
| T-01-15 | Information Disclosure | locale JSON content | low | accept | Locale files are static, non-secret, publicly bundled client assets by design — no mitigation needed beyond normal build hygiene |
</threat_model>

<verification>
Detection/direction behavior tests pass; every locale file has an identical, complete key set matching the exact language list from the go/no-go decision.
</verification>

<success_criteria>
LANG-01 auto-detection works with no mapping-table shortcut, and LANG-07's "all interface strings localized" is mechanically verifiable (key-set parity check) rather than assumed.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `src/server/i18n/detect.ts` — `SUPPORTED_LANGUAGES`, `detectLanguage`
- `src/server/i18n/dir.ts` — `dirFor`
- `src/lib/i18n/format.ts` — shared ASCII-digit formatter
- `src/lib/i18n/locales/{en,ar,es,fr,pt,hi,zh,ru,id[,sw]}.json` — complete localized key sets

<output>
Create `.planning/phases/01-foundation-and-the-realtime-spine/01-05-SUMMARY.md` when done
</output>
