---
phase: 01-foundation-and-the-realtime-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - components.json
  - drizzle.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/server/db/schema.ts
  - drizzle/0000_init.sql
  - scripts/migrate.mjs
  - docker-compose.yml
  - src/app/api/health/route.ts
  - .env.example
  - .gitignore
autonomous: false
requirements: [FOUND-03, FOUND-04, ID-05]
must_haves:
  truths:
    - "The database schema contains no column capable of storing a name, email, phone number, or raw IP address for any visitor — the only IP-derived data anywhere in the system is a transient HMAC key string used as a rate_limit_buckets row key, never a persisted address column."
    - "The responders table and a nullable conversations.assigned_responder_id foreign key exist in the schema and first migration, even though nothing in the running app writes a non-null value to it yet (FOUND-03)."
    - "A fresh clone with no prior state reaches a working local dev server backed by a real Postgres database via one documented command sequence (docker compose up -d db, then npm install, then npm run dev)."
    - "GET /api/health performs a real SELECT 1 against Postgres and returns 200 when the database is reachable, not a hardcoded 200."
  artifacts:
    - path: "src/server/db/schema.ts"
      provides: "Drizzle schema for all 7 tables: visitors, conversations, messages, responders, push_subscriptions, message_translations, rate_limit_buckets"
    - path: "scripts/migrate.mjs"
      provides: "Applies drizzle/*.sql via drizzle-orm/postgres-js/migrator at process/container start"
    - path: "src/app/api/health/route.ts"
      provides: "GET /api/health — SELECT 1 liveness probe, force-dynamic"
  key_links:
    - from: "scripts/migrate.mjs"
      to: "drizzle/0000_init.sql"
      via: "migrate(db, { migrationsFolder: './drizzle' })"
      pattern: "migrationsFolder"
    - from: "src/app/api/health/route.ts"
      to: "src/server/db/schema.ts"
      via: "postgres.js sql`select 1` against the same DATABASE_URL the schema was migrated into"
      pattern: "select 1"
  prohibitions:
    - requirement_id: "ID-05"
      category: "privacy"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT add a name, email, phone, or raw-IP column to any table in this schema. Canon PII-minimization item — breadcrumbed to /gsd-secure-phase; enforced here structurally by omission from the schema."
  assumptions:
    - "ID-05 'encoding/equality' edge (from 01-EDGE-PROBE.json) does not meaningfully apply — ID-05 is a negative constraint (absence of a column), not a text-comparison operation. Left as a flagged assumption, not forced into a truth."
---

<objective>
Stand up the Walking Skeleton: a pinned Next.js 16 project, the complete 7-table Drizzle schema (including the FOUND-03 forward-compatible `responders`/assignment columns), a working local Postgres dev loop, and a real `/api/health` DB round trip. This is the foundation every other Phase 1 plan builds on — nothing here implements chat, auth, or i18n logic; it proves the stack is wired end to end.

Purpose: De-risk the framework/DB/migration boot sequence before any product logic is written on top of it, per Walking Skeleton mode.
Output: A running `npm run dev` app backed by a real migrated Postgres database, `SKELETON.md` (already written), and the schema every later plan imports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Confirm SUS-flagged package versions before install</name>
  <action>Pause before running npm install and ask the human to confirm the four [SUS]-flagged package versions (next, typescript, openai, tailwindcss) against the npm registry, per RESEARCH.md's Package Legitimacy Audit.</action>
  <what-built>Nothing yet — this checkpoint runs before any `npm install`, gating the package versions this whole phase depends on.</what-built>
  <how-to-verify>
    01-RESEARCH.md's Package Legitimacy Audit flags four packages as `[SUS]` by a "too-new" heuristic (all confirmed false positives — official repos, huge download counts, no postinstall scripts): `next` (16.2.10), `typescript` (6.0.3 is the pin, but `typescript@latest` recently jumped to 7.0.2), `openai` (6.48.0), `tailwindcss` (4.3.3).
    1. Visit https://www.npmjs.com/package/next and confirm the latest version is close to `16.2.10` and the maintainer is the Vercel org.
    2. Visit https://www.npmjs.com/package/typescript and confirm `6.0.3` exists in the version history (we pin this exact version, not `latest`).
    3. Visit https://www.npmjs.com/package/openai and confirm `6.48.0` exists and the maintainer is `openai`.
    4. Visit https://www.npmjs.com/package/tailwindcss and confirm `4.3.3` exists and the maintainer is `tailwindlabs`.
  </how-to-verify>
  <resume-signal>Type "approved" once all four package versions are confirmed legitimate, or describe any discrepancy found.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Scaffold pinned Next.js 16 + TypeScript 6.0.3 + Tailwind v4 + shadcn project</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, components.json, src/app/globals.css, .env.example, .gitignore</files>
  <read_first>
    .claude/CLAUDE.md (Installation section, Docker standalone constraints)
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Standard Stack table + Installation block, lines ~114-169)
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Design System section, font decision, shadcn preset string)
  </read_first>
  <action>
    Hand-author package.json with exact pinned versions (do not rely on `create-next-app`'s own version resolution, which can silently grab newer majors): dependencies next@16.2.10, react@19.2.7, react-dom@19.2.7, drizzle-orm@0.45.2, postgres@3.4.9, jose@6.2.3, @node-rs/argon2@2.0.2, openai@6.48.0, zod@4.4.3, tailwindcss@4.3.3, @tailwindcss/postcss; devDependencies typescript@6.0.3 (exact, no caret), @types/node, @types/react, @types/react-dom, drizzle-kit@0.31.10. Add npm scripts: dev (next dev), build (next build), start (next start), migrate (node scripts/migrate.mjs). Run npm install. Author tsconfig.json for strict mode + the `src/` baseUrl/paths alias (`@/*` -> `./src/*`). Author next.config.ts with output: 'standalone' and compress: false (per CLAUDE.md's SSE compression pitfall — do this now, not deferred, since it is a one-line config that later SSE plans depend on). Run the shadcn init command exactly as locked in UI-SPEC.md: `npx shadcn init --template next --base radix --css-variables --rtl -y`, then confirm/edit components.json so `baseColor` is `"neutral"` and `tailwind.config` is empty string (Tailwind v4 has no config file). Author src/app/globals.css with `@theme inline` per Tailwind v4 convention and the system font stack from UI-SPEC.md's font decision (no next/font, no webfont). Run `npx shadcn add button drawer input label textarea` (the only Registry Safety blocks approved for this phase per UI-SPEC.md). Add .env.example documenting DATABASE_URL, SESSION_SECRET, SETUP_TOKEN, OVH_API_KEY, OVH_BASE_URL (values blank/placeholder, real values are runtime secrets never committed). Add .gitignore covering node_modules, .next, .env, .env.local.
  </action>
  <acceptance_criteria>
    package.json devDependencies.typescript is exactly "6.0.3" (no ^ or ~ prefix).
    npx tsc --version prints Version 6.0.3.
    components.json exists and its "tailwind.baseColor" value is "neutral".
    src/app/globals.css contains the literal string "@theme inline".
    src/components/ui/ contains button, drawer, input, label, and textarea component files after the shadcn add command.
    npm run build exits 0 (validates the scaffold compiles, even with placeholder page content).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.devDependencies.typescript!=='6.0.3'){console.error('typescript not pinned to 6.0.3');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>package.json is pinned exactly per RESEARCH.md, shadcn is initialized with --rtl, globals.css uses @theme inline with the system font stack, and npm run build succeeds.</done>
</task>

<task type="auto">
  <name>[BLOCKING] Task 2: Full 7-table Drizzle schema + migration generation + local Postgres</name>
  <files>src/server/db/schema.ts, drizzle.config.ts, drizzle/0000_init.sql, scripts/migrate.mjs, docker-compose.yml</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Pattern 8 migration-at-boot, lines ~549-562; Architectural Responsibility Map's push_subscriptions/message_translations schema-only note; Pattern 7 rate_limit_buckets SQL shape, lines ~529-547)
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-14's dependence on a `responders` existence check; FOUND-03)
    .claude/CLAUDE.md (postgres.js LISTEN/NOTIFY driver decision, Coolify named-volume expectations)
  </read_first>
  <action>
    In src/server/db/schema.ts define exactly 7 Drizzle tables using drizzle-orm/pg-core: (1) visitors — uuid primary key default gen_random_uuid(), lang text, appearance text, created_at timestamptz default now(), last_seen_at timestamptz default now(); (2) conversations — serial/uuid primary key, visitor_id references visitors.id, status text not null default 'new' (values: new, in_progress, closed), assigned_responder_id nullable integer/uuid references responders.id (FOUND-03 forward-compatible column, never written to this phase), created_at, updated_at; add a partial unique index on conversations(visitor_id) WHERE status <> 'closed' so CHAT-08 can resolve "the" open conversation for a returning visitor; (3) messages — bigserial/serial primary key (this becomes both the SSE event id and the Last-Event-ID cursor — must be a monotonic integer, not a uuid), conversation_id references conversations.id, sender text not null (values: visitor, owner), body text not null, client_msg_id text (nullable, for send idempotency), created_at timestamptz default now(); (4) responders — serial/uuid primary key, email text unique not null, password_hash text not null, display_name text, is_online boolean not null default false (D-07 presence read path), created_at; (5) push_subscriptions — serial primary key, visitor_id references visitors.id, endpoint text unique not null, p256dh text not null, auth text not null, created_at, last_success_at nullable, failure_count integer not null default 0 (schema only — Phase 2 writes to this, nothing in Phase 1 does); (6) message_translations — serial primary key, message_id references messages.id, target_lang text not null, translated_text text, status text not null default 'pending' (schema only — Phase 2 writes to this); (7) rate_limit_buckets — text primary key (the key column, e.g. 'v:<uuid>' or 'ip:<hmac-hex>' — never a raw IP), tokens real not null, updated_at timestamptz not null default now(). Author drizzle.config.ts pointing schema at src/server/db/schema.ts, dialect postgresql, out ./drizzle, using DATABASE_URL from env. Run npx drizzle-kit generate to emit drizzle/0000_init.sql (do not hand-write the SQL). Author scripts/migrate.mjs per RESEARCH.md Pattern 8 exactly: a non-pooled postgres client (max: 1), drizzle-orm/postgres-js/migrator's migrate(db, { migrationsFolder: './drizzle' }), then migrationClient.end(). Author docker-compose.yml with a single postgres:17 service, a named volume for its data directory (this is what OPS-06 verifies later in Plan 01-13), and POSTGRES_DB/POSTGRES_USER/POSTGRES_PASSWORD matching .env.example's DATABASE_URL. Run docker compose up -d db, wait for readiness, then node scripts/migrate.mjs against it to prove the migration applies cleanly.
  </action>
  <acceptance_criteria>
    src/server/db/schema.ts exports exactly 7 pgTable definitions: visitors, conversations, messages, responders, pushSubscriptions, messageTranslations, rateLimitBuckets.
    drizzle/0000_init.sql exists and contains CREATE TABLE statements for all 7 table names.
    scripts/migrate.mjs runs to completion against the local docker-compose Postgres with exit code 0.
    A post-migration query (information_schema.tables) against the local DB returns exactly 7 tables in the public schema.
    conversations has a partial unique index whose definition includes "status" and "<>" (not a plain unique constraint on visitor_id alone).
  </acceptance_criteria>
  <verify>
    <automated>test -f drizzle/0000_init.sql && grep -ci '^create table' drizzle/0000_init.sql | grep -qE '^[7-9]$|^[1-9][0-9]+$' && echo ok</automated>
  </verify>
  <done>7-table schema is defined, one migration file is generated and committed, migrate.mjs applies it successfully to a running local Postgres, and the FOUND-03 nullable assignment column exists on conversations.</done>
</task>

<task type="auto">
  <name>Task 3: Health check route, minimal app shell, documented local run command</name>
  <files>src/app/api/health/route.ts, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    .claude/CLAUDE.md (health check section — force-dynamic, SELECT 1, keep out of proxy matcher)
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Recommended Project Structure tree, lines ~255-295)
  </read_first>
  <action>
    Create src/app/api/health/route.ts exporting `export const dynamic = 'force-dynamic'` and a GET handler that runs a real `sql\`select 1\`` against the same postgres.js client configuration scripts/migrate.mjs used, returning 200 with a small JSON body on success and 503 on any DB error — this is a real liveness check, not a hardcoded 200. Create a minimal src/app/layout.tsx (plain html/body, no cookie logic yet — that is Plan 01-06's job) and src/app/page.tsx rendering a placeholder heading ("One Chat") so the dev server has something to serve. Do not build any chat, auth, or i18n logic here — this task exists only to prove the stack boots and reads the database. Document the local full-stack run command as a comment block at the top of docker-compose.yml: `docker compose up -d db && npm run dev`, then `curl localhost:3000/api/health`.
  </action>
  <acceptance_criteria>
    src/app/api/health/route.ts contains the literal export const dynamic = 'force-dynamic'.
    src/app/api/health/route.ts contains a call to the postgres.js sql tagged template with "select 1" (case-insensitive).
    npm run build succeeds with the real schema and health route in place.
    Running `docker compose up -d db && npm run dev` locally and curling /api/health returns HTTP 200 with a JSON body containing an "ok" or "status" key.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "force-dynamic" src/app/api/health/route.ts | grep -qE '^[1-9]' && grep -ci "select 1" src/app/api/health/route.ts | grep -qE '^[1-9]' && npm run build && echo ok</automated>
  </verify>
  <done>/api/health performs a real DB read and returns 200/503 accordingly; the app builds; the local run command is documented and has been exercised at least once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem/env → app config | DATABASE_URL, SESSION_SECRET, SETUP_TOKEN, OVH_API_KEY are read from process env, never hardcoded |
| Public health endpoint | /api/health is unauthenticated by design (Coolify healthcheck target) — must leak nothing beyond liveness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | src/app/api/health/route.ts | low | mitigate | Health response body contains only a boolean/status field, never a stack trace, connection string, or query result |
| T-01-02 | Tampering | npm install (package legitimacy) | high | mitigate | Blocking human-verify checkpoint before install for the four [SUS]-flagged packages (next, typescript, openai, tailwindcss), per RESEARCH.md's Package Legitimacy Audit |
| T-01-03 | Information Disclosure | src/server/db/schema.ts | medium | mitigate | Schema structurally omits any name/email/phone/raw-IP column for visitors — enforced by the prohibition above, not just by convention |
| T-01-04 | Tampering | .env.example vs .env | high | mitigate | .gitignore excludes .env/.env.local; .env.example carries only placeholder values, never real secrets |
</threat_model>

<verification>
`npm run build` succeeds; `node scripts/migrate.mjs` applies cleanly to a fresh local Postgres started via `docker compose up -d db`; `/api/health` returns 200 after migration; schema.ts exports exactly 7 tables matching FOUND-03's forward-compatible design.
</verification>

<success_criteria>
A developer with a fresh clone can run `docker compose up -d db && npm install && node scripts/migrate.mjs && npm run dev`, then load `http://localhost:3000` and `http://localhost:3000/api/health`, and see a real DB-backed 200 response. This is the foundation every subsequent Phase 1 plan builds on.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `package.json` — pinned dependency manifest (next@16.2.10, react@19.2.7, react-dom@19.2.7, drizzle-orm@0.45.2, postgres@3.4.9, jose@6.2.3, @node-rs/argon2@2.0.2, openai@6.48.0, zod@4.4.3, tailwindcss@4.3.3, typescript@6.0.3 exact, drizzle-kit@0.31.10)
- `tsconfig.json`, `next.config.ts` (output: standalone, compress: false), `postcss.config.mjs`, `components.json` (shadcn, baseColor neutral, rtl)
- `src/app/globals.css` (@theme inline, system font stack)
- `src/components/ui/{button,drawer,input,label,textarea}.tsx` (shadcn-generated)
- `src/server/db/schema.ts` — exports `visitors`, `conversations`, `messages`, `responders`, `pushSubscriptions`, `messageTranslations`, `rateLimitBuckets`
- `drizzle.config.ts`, `drizzle/0000_init.sql`
- `scripts/migrate.mjs`
- `docker-compose.yml` (postgres:17, named volume)
- `src/app/api/health/route.ts` (GET, force-dynamic, real SELECT 1)
- `src/app/layout.tsx`, `src/app/page.tsx` (placeholder, superseded by Plans 01-06/01-12)
- `.env.example`, `.gitignore`

<output>
Create `.planning/phases/01-foundation-and-the-realtime-spine/01-01-SUMMARY.md` when done
</output>
