---
phase: 01-foundation-and-the-realtime-spine
plan: 13
type: execute
wave: 7
depends_on: ["01-01", "01-11", "01-12"]
files_modified:
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - next.config.ts
autonomous: false
requirements: [OPS-06, OPS-09, FOUND-04]
user_setup:
  - service: coolify
    why: "The product's constraint is self-hosted Docker on a Coolify instance the owner controls — Claude has no CLI/API credential to an environment it doesn't own"
    env_vars:
      - name: DATABASE_URL
        source: "Coolify's managed Postgres resource connection string — mark runtime-only, never build-time"
      - name: SESSION_SECRET
        source: "Generate a strong random value (e.g. openssl rand -hex 32) — mark runtime-only"
      - name: SETUP_TOKEN
        source: "The same value configured in Plan 01-07 — mark runtime-only"
      - name: OVH_API_KEY
        source: "From the OVH Public Cloud console (Plan 01-02's prerequisite) — mark runtime-only; not called by the running app in Phase 1, but kept consistent for Phase 2"
      - name: IP_HASH_SECRET
        source: "Generate a strong random value for rate-limit IP hashing (Plan 01-08) — mark runtime-only, owner-rotatable"
    dashboard_config:
      - task: "Create a new Coolify application from this repository, build pack = Dockerfile (not Nixpacks)"
        location: "Coolify dashboard -> Applications -> New -> Dockerfile"
      - task: "Set Ports Exposes = 3000"
        location: "Coolify application settings -> Network"
      - task: "Attach a managed Postgres resource with a named volume, and connect it to this application's network"
        location: "Coolify dashboard -> Databases -> New Postgres -> Connect to Predefined Network"
      - task: "Configure the healthcheck path as /api/health"
        location: "Coolify application settings -> Health Checks"
must_haves:
  truths:
    - "docker build followed by docker run against a fresh Postgres produces a running app that serves /api/health as 200 after migrations apply automatically at container start — no manual migration step required (FOUND-04)."
    - "A deliberate docker restart (or Coolify redeploy) of the Postgres service leaves every previously stored conversation and message row intact and queryable afterward, verified via a post-restart row-count check against a named volume (OPS-06)."
    - "With log_statement=none set on Postgres, sending a message with pastoral/faith content produces zero occurrences of that message text anywhere in docker logs for either the app or Postgres containers (OPS-09)."
    - "next.config.ts's compress: false (set in Plan 01-01) and the SSE routes' X-Accel-Buffering: no header (set in Plan 01-08) are both present in the built image, unchanged by the Docker build process."
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage (deps -> builder -> runner), node:24-alpine, standalone output, non-root user, migrate-then-start CMD"
  key_links:
    - from: "Dockerfile CMD"
      to: "scripts/migrate.mjs"
      via: "sh -c 'node ./scripts/migrate.mjs && node server.js'"
      pattern: "migrate.mjs && node server.js"
  prohibitions:
    - requirement_id: "OPS-09"
      category: "privacy"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT let pastoral message text reach container logs (app or Postgres) under any code path — log_statement=none at the Postgres config level, plus the parameterized-queries discipline already enforced in every repo module (Plan 01-03) and write route (Plan 01-08); no console.log of a message body anywhere in the codebase."
  assumptions: []
---

<objective>
Package the app as a single Dockerfile-built container per CLAUDE.md's exact specification, verify it locally, and walk the owner through the one genuinely owner-only step — connecting it to their Coolify instance — followed by a verification checkpoint proving OPS-06 (named-volume restart survival) and OPS-09 (no pastoral content in logs) hold on the real deployment.

Purpose: FOUND-04 and roadmap success criterion 6 ("the app builds and deploys as a single Coolify container with migrations applied at start") close out the phase.
Output: `Dockerfile`, a verified local `docker build`/`docker run`, and a deployed Coolify application.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-stage Dockerfile + local build/run verification</name>
  <files>Dockerfile, .dockerignore, docker-compose.yml, next.config.ts</files>
  <read_first>
    .claude/CLAUDE.md ("Next.js standalone Docker build for Coolify" section, in full — the exact deps/builder/runner stages, the .next/static and public copy-manually trap, the drizzle/ folder copy trap, build-time vs runtime env var distinction, the CMD shape, Ports Exposes = 3000)
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Pitfall 6 — deployment loses conversations before there's anything to lose; OPS-06/OPS-09 mitigation)
  </read_first>
  <action>
    Author a multi-stage Dockerfile: (deps) node:24-alpine, copy package.json/package-lock.json, npm ci; (builder) copy deps' node_modules plus the full source, run npm run build (output: standalone per next.config.ts, already set in Plan 01-01); (runner) node:24-alpine, create a non-root nextjs user, copy .next/standalone, then MANUALLY copy .next/static into .next/standalone/.next/static and public/ into .next/standalone/public (the standalone trace does not include these — the classic unstyled-page failure mode), and MANUALLY copy the drizzle/ folder and scripts/migrate.mjs into the runner image (also not traced), set CMD ["sh","-c","node ./scripts/migrate.mjs && node server.js"]. Add a HEALTHCHECK or document that Coolify's own healthcheck config (Task 2) points at /api/health. Author .dockerignore excluding node_modules, .next, .git, .env*. Update docker-compose.yml's local dev setup to optionally build from this Dockerfile (a second service alongside the existing db service) so `docker compose up --build` exercises the exact same image locally that ships to Coolify. Run docker build locally, then docker run against the local docker-compose Postgres, and curl /api/health to confirm 200 with migrations having applied automatically (no manual migrate step invoked outside the container).
  </action>
  <acceptance_criteria>
    The Dockerfile has three distinct stages (deps, builder, runner) each with a FROM node:24-alpine (or FROM <deps-stage> as appropriate).
    The runner stage explicitly copies .next/static, public/, and drizzle/ (three separate COPY instructions beyond the standalone trace).
    The CMD instruction runs scripts/migrate.mjs before node server.js in the same shell invocation.
    A local docker build && docker run against the compose Postgres serves /api/health as 200 with zero manual migration commands run outside the container.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^FROM" Dockerfile | grep -qE '^3$' && grep -c "COPY.*\.next/static\|COPY.*static" Dockerfile | grep -qE '^[1-9]' && grep -c "migrate.mjs" Dockerfile | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>The image builds in three stages, correctly copies everything the standalone trace omits, migrates automatically at start, and has been verified locally end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Postgres logging discipline (OPS-09) + named-volume compose config</name>
  <files>docker-compose.yml</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Pitfall 6/OPS-09 — log_statement=none plus parameterized writes)
  </read_first>
  <action>
    Set log_statement=none (and log_min_duration_statement appropriately conservative, not logging statement text) in the docker-compose Postgres service's command/config, confirm the named volume from Plan 01-01 is present and will be the equivalent of what Coolify's managed Postgres resource provides (document in the SUMMARY the exact Coolify-side equivalent: attaching a managed Postgres resource with a persistent volume, per this plan's user_setup dashboard_config). Send a test message containing an identifiable pastoral-content string through the running local stack, then grep the app and Postgres container logs for that exact string, confirming zero matches.
  </action>
  <acceptance_criteria>
    docker-compose.yml's Postgres service configuration includes log_statement=none.
    A grep of `docker compose logs` for a known test message's exact text returns zero matches after sending that message through the running app.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "log_statement" docker-compose.yml | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>Postgres is configured to never log statement text, and a live test confirms no pastoral content reaches either container's logs.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Connect and deploy the app on the owner's Coolify instance</name>
  <action>Pause and ask the owner to create the Coolify application (Dockerfile build pack), attach a managed Postgres resource with a named volume, configure the five runtime-only env vars, set the healthcheck path, and deploy — this requires dashboard access this environment does not have.</action>
  <what-built>A verified Docker image (Task 1) and confirmed logging discipline (Task 2) — the app is ready to deploy, but connecting it to the owner's actual Coolify instance requires dashboard access this environment does not have.</what-built>
  <how-to-verify>
    1. Create a new Coolify application from this repository, build pack = Dockerfile (not Nixpacks, per CLAUDE.md's explicit warning that Nixpacks hides the standalone-copy and migration-at-start steps this app needs).
    2. Set Ports Exposes = 3000 in the application's network settings.
    3. Attach a managed Postgres resource with a named/persistent volume, connected to this application's predefined network.
    4. Configure the five env vars listed in this plan's user_setup as runtime-only (never build-time): DATABASE_URL, SESSION_SECRET, SETUP_TOKEN, OVH_API_KEY, IP_HASH_SECRET.
    5. Configure the healthcheck path as /api/health.
    6. Deploy.
  </how-to-verify>
  <resume-signal>Reply with the deployed URL once the Coolify application is live, or describe any deployment error encountered.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the live deployment — health, direct render, send/reply, restart persistence</name>
  <action>Pause and ask the owner to check the deployed health endpoint, confirm direct chat render and the send/reply round trip, and deliberately restart the Postgres resource to verify OPS-06 persistence before signing off on this phase's final success criteria.</action>
  <what-built>A live Coolify deployment of One Chat.</what-built>
  <how-to-verify>
    1. Visit `https://<your-deployed-url>/api/health` and confirm it returns 200.
    2. Visit `https://<your-deployed-url>/` and confirm the chat renders directly (CHAT-01), the header shows exactly two controls, and the welcome renders.
    3. Send a test message from the visitor side; confirm it appears; then log in at `/admin/login` (after completing `/admin/setup` once, per Plan 01-07) and confirm the conversation is visible and a reply can be sent and arrives on the visitor side.
    4. Deliberately restart the Postgres resource/container from the Coolify dashboard, then reload the visitor chat and confirm the conversation history is still present (OPS-06).
    5. Confirm the locale files shipped in this build match exactly the language list decided in `.planning/phases/01-foundation-and-the-realtime-spine/TRANSLATION-SPIKE-GO-NO-GO.md` (closing roadmap success criterion 1's "every locale file in the build reflects that answer rather than the assumed ten").
  </how-to-verify>
  <resume-signal>Type "approved" once health check, direct chat render, send/reply round trip, and the Postgres-restart persistence check all pass, or describe any failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coolify env var configuration | Build-time vs runtime distinction is security-relevant — a secret marked build-time is baked into an image layer, recoverable from the image itself |
| Public deployed URL | The one externally reachable surface — must present identical trust properties to the local dev stack (no debug endpoints, no verbose error pages) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-35 | Information Disclosure | Coolify build-time vs runtime env vars | high | mitigate | This plan's user_setup explicitly marks DATABASE_URL, SESSION_SECRET, SETUP_TOKEN, OVH_API_KEY, and IP_HASH_SECRET as runtime-only — the checkpoint instructs the owner to verify each is unchecked from "build" in the Coolify UI |
| T-01-36 | Information Disclosure | Postgres query logs | medium | mitigate | log_statement=none, verified by a live grep-for-zero-matches test against a known message string (Task 2) |
| T-01-37 | Tampering | Coolify build pack choice | medium | mitigate | Dockerfile build pack explicitly required over Nixpacks, since Nixpacks would silently skip the standalone-copy and migrate-at-start steps this app depends on |
</threat_model>

<verification>
Local docker build/run serves /api/health 200 with automatic migration; Postgres logs contain zero pastoral content after a live test; the owner-verified Coolify deployment passes health check, direct render, send/reply round trip, and the deliberate-restart persistence check.
</verification>

<success_criteria>
FOUND-04 and OPS-06/OPS-09 are closed, and roadmap Phase 1 success criterion 6 ("the app builds and deploys as a single Coolify container with migrations applied at start") is verified against a real deployment, not just a local build.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `Dockerfile` — multi-stage, standalone-aware, migrate-then-start
- `.dockerignore`
- `docker-compose.yml` (log_statement=none, app+db services)
- A live Coolify deployment (external to this repository)

<output>
Create `.planning/phases/01-foundation-and-the-realtime-spine/01-13-SUMMARY.md` when done
</output>
