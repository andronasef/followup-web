---
phase: 01-foundation-and-the-realtime-spine
plan: 10
type: execute
wave: 5
depends_on: ["01-08"]
files_modified:
  - src/components/chat/MessageBubble.tsx
  - src/components/chat/MessageList.tsx
  - src/components/chat/Composer.tsx
  - src/lib/chat/useChatStream.ts
autonomous: true
requirements: [CHAT-03, CHAT-04, LANG-04, LANG-05]
must_haves:
  truths:
    - "Attempting to send an empty/whitespace-only composer value is a no-op — the send button is disabled/inert until the composer has non-empty content."
    - "The bubble appears the instant the visitor taps send, at 60% opacity, and settles to 100% opacity only once the server confirms durable persistence (D-18) — never before."
    - "On send failure, automatic retry happens quietly first; only after retries are exhausted does the specific bubble change to a failed state with tap-to-retry — the visitor's typed text is never cleared from the screen at any point (D-19)."
    - "Exactly two delivery states exist and are ever rendered — sent (a Check icon) and failed — never a 'delivered' or 'seen' state (D-20)."
    - "A message containing only a bare URL or only a scripture reference with no surrounding text still renders correctly bidi-isolated inside an RTL conversation, via dir=auto plus unicode-bidi: isolate on the message text node (LANG-04)."
    - "Only the UI-SPEC-allowlisted icons (SendHorizontal for send, RotateCcw for retry) mirror under dir=rtl in this component set — timestamps and digits never mirror or reorder, and no blanket RTL icon-mirroring rule exists (LANG-05)."
    - "The thread anchors to the newest message on open and on a new message only when the visitor is already scrolled to the bottom — it never yanks the view away from someone scrolled up reading history."
    - "The composer textarea grows to a maximum of 5 lines then scrolls internally — the send button and message list never leave the viewport."
    - "A reconnect with the client's own last-received Last-Event-ID replays exactly the messages missed and nothing already shown — no duplicate render, no gap in the rendered list (CHAT-07, client half of Plan 01-08's server contract)."
    - "The ~4-minute deliberate server-side stream recycle (D-15) produces zero UI — the client's EventSource reconnects and replays silently; only after repeated CONSECUTIVE connection failures does a quiet 'Reconnecting…' Label line appear (D-17)."
    - "The D-16 polling fallback is present as a client-side flag but defaults to off — EventSource is the only active transport this phase."
    - statement: "A single unbroken 500-character token and a 200-character URL wrap inside the bubble without horizontal page scroll, in both dir=ltr and dir=rtl."
      verification: backstop
    - statement: "Bidi isolation is verified against actual grapheme-level rendering (not just code-point counting) for the Arabic-paragraph-with-embedded-URL fixture from RESEARCH.md's Pitfall 5."
      verification: backstop
    - statement: "While the owner is online and the SSE stream is connected, an owner reply renders in the message list without a page refresh (CHAT-04, client half)."
      verification: backstop
  artifacts:
    - path: "src/components/chat/MessageBubble.tsx"
      provides: "Bidi-isolated message text, sent/failed delivery states only, icon-allowlist compliant"
    - path: "src/components/chat/MessageList.tsx"
      provides: "Scroll-anchor-on-bottom behavior"
    - path: "src/components/chat/Composer.tsx"
      provides: "Grow-to-5-lines textarea, optimistic send with client_msg_id, auto-retry, failed+tap-to-retry"
    - path: "src/lib/chat/useChatStream.ts"
      provides: "EventSource client with Last-Event-ID persistence, silent reconnect, D-16 polling-fallback flag (off)"
  key_links:
    - from: "src/components/chat/Composer.tsx"
      to: "src/app/api/chat/messages/route.ts"
      via: "POST fetch carrying a client-generated client_msg_id for idempotent retry"
      pattern: "client_msg_id"
    - from: "src/lib/chat/useChatStream.ts"
      to: "src/app/api/chat/stream/route.ts"
      via: "new EventSource('/api/chat/stream'), lastEventId persisted across reconnects"
      pattern: "EventSource"
  prohibitions:
    - requirement_id: "CHAT-03"
      category: "values"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT clear, discard, or overwrite a visitor's typed message text on send failure or during a retry cycle — the exact text they wrote must remain visible/editable until they choose to change it (D-19). Someone who just typed something hard to say must never have to type it twice."
    - requirement_id: "LANG-05"
      category: "values"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT apply a blanket RTL icon-mirroring rule in Composer.tsx or MessageBubble.tsx — only SendHorizontal and RotateCcw mirror, applied via an explicit per-icon class, never a global [dir=rtl] selector."
  assumptions: []
---

<objective>
Build the composer, message list/bubble, and the EventSource client hook — the components that make CHAT-03's optimistic-send/single-delivery-state contract and CHAT-04's live-arrival contract real on the client, plus LANG-04's bidi isolation for mixed-direction content. Standalone from Plan 01-09's shell components (different files), so both plans run in the same wave.

Purpose: D-18/D-19/D-20's failure UX contract is the most behaviorally exacting part of this phase — a message a visitor "just typed something hard to say" must never be lost.
Output: `components/chat/{MessageBubble,MessageList,Composer}.tsx`, `lib/chat/useChatStream.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MessageBubble and MessageList — bidi isolation, scroll anchor, delivery states</name>
  <files>src/components/chat/MessageBubble.tsx, src/components/chat/MessageList.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Icon mirroring allowlist table; Color's accent-for-outgoing-bubble-only rule; Typography's Body role; long-text/message-bubble backstop row; partial/mixed-direction-message row)
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md ("Don't Hand-Roll" table's bidi isolation row — <bdi>/unicode-bidi: isolate, dir derived per-message, never a hand-rolled regex reordering)
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-20 — exactly two visible states, sent/failed, never delivered/seen)
  </read_first>
  <action>
    Create src/components/chat/MessageBubble.tsx: renders a single message with dir="auto" and unicode-bidi: isolate (or a <bdi> wrapper) on the text node specifically, so an embedded Latin URL or scripture reference inside an Arabic message renders intact regardless of the bubble's own ambient direction. Visitor-sent bubbles use --primary background/foreground (the accent color, per UI-SPEC.md's rule that this is one of only three accent usages in the whole product); owner-sent bubbles use --muted. Render exactly two possible delivery-state indicators for the visitor's own outgoing messages: a Check icon at --muted-foreground (state 'sent') or a failed state with a RotateCcw retry icon (state 'failed') — no third state, ever, and no "delivered"/"seen" anywhere in this component. RotateCcw is the only mirrored icon in this file (per the allowlist); Check is not mirrored. Timestamps render via the shared ASCII-digit formatter from Plan 01-05, never locale-native digit glyphs. Create src/components/chat/MessageList.tsx: renders an ordered list of MessageBubble instances, tracks whether the user is scrolled to the bottom, and auto-scrolls to the newest message on mount and on a new message ONLY when already at the bottom before the new message arrived — never yanking a scrolled-up reader's view.
  </action>
  <acceptance_criteria>
    MessageBubble.tsx's message-text element has dir="auto" and either a unicode-bidi: isolate style or a <bdi> wrapper.
    MessageBubble.tsx contains no string "delivered" or "seen" used as a rendered delivery-state label.
    MessageBubble.tsx uses the shared digit formatter (imports from src/lib/i18n/format.ts) for timestamp rendering, not a raw toLocaleString call.
    MessageList.tsx's auto-scroll logic is conditioned on an at-bottom check before applying scroll-to-newest on a new-message event (not an unconditional scrollIntoView on every update).
  </acceptance_criteria>
  <verify>
    <automated>! grep -iE '"delivered"|"seen"' src/components/chat/MessageBubble.tsx && grep -c "dir=\"auto\"\|dir={.auto.}\|bdi" src/components/chat/MessageBubble.tsx | grep -qE '^[1-9]' && grep -c "format" src/components/chat/MessageBubble.tsx | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>Bidi isolation is per-message-text-node, delivery states are exactly sent/failed, digits are always ASCII, and scroll never yanks a reading visitor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composer — optimistic send, auto-retry, failed+tap-to-retry</name>
  <files>src/components/chat/Composer.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-18/D-19 in full — optimistic bubble at faint opacity, automatic retry before surfacing anything, visitor text never lost)
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Composer safe-area rule; Copywriting Contract's exact "Couldn't send. Tap to try again." and rate-limited copy; overflow/composer-growth row)
    src/app/api/chat/messages/route.ts (from Plan 01-08 — the POST contract, client_msg_id idempotency)
  </read_first>
  <behavior>
    - Test: submitting an empty or whitespace-only composer value does not call the send API and does not create an optimistic bubble.
    - Test: submitting non-empty text immediately produces a locally-rendered optimistic bubble at 60% opacity, before the network call resolves.
    - Test: on a successful response, the optimistic bubble's opacity settles to 100% and its state becomes 'sent'.
    - Test: on repeated network failure, the composer retries automatically a few times before showing a failed state, and the original typed text is never cleared or overwritten during any of these retries.
    - Test: tapping "tap to retry" on a failed bubble resubmits using the same client_msg_id (so a duplicate row is never created server-side per Plan 01-08's idempotency).
  </behavior>
  <action>
    Create src/components/chat/Composer.tsx: a textarea that grows up to 5 lines (measured via scrollHeight, capping at a max-height matching 5 lines of the 16px/1.5 body type scale) then scrolls internally, with pb-[env(safe-area-inset-bottom)] added to the composer bar's padding. The send button/icon (SendHorizontal, the one mirrored icon here) is --muted-foreground on transparent when the composer is empty and switches to the accent-colored active state only when non-empty (per UI-SPEC.md's accent-budget rule). On submit: guard against empty/whitespace-only input (no-op, no API call). Generate a client_msg_id (e.g., crypto.randomUUID()) once per composed message, render an optimistic bubble immediately at 60% opacity in 'sending' visual state, POST to /api/chat/messages with { body, clientMsgId }. On success, update that exact bubble's state to 'sent' and opacity to 100%. On failure, retry the same POST (same clientMsgId, so a retry is idempotent server-side) a small fixed number of times with a short backoff, silently — no UI change during these retries. Only after retries are exhausted, change that one bubble's state to 'failed' with the exact locked copy ("Couldn't send. Tap to try again.") and a tap-to-retry affordance that resubmits with the identical clientMsgId. At no point does the composer's text field get cleared, reset, or repopulated from the failed message — the visitor typed once.
  </action>
  <acceptance_criteria>
    All five behavior tests pass.
    The failed-state string in Composer.tsx matches exactly "Couldn't send. Tap to try again." (or the locale-JSON-sourced equivalent key).
    The clientMsgId used for a retry is provably the same value used for the original attempt (not regenerated per retry).
  </acceptance_criteria>
  <verify>
    <automated>node --test src/components/chat/Composer.test.* 2>&1 | tail -30 || npm test -- --testPathPattern=Composer 2>&1 | tail -30</automated>
  </verify>
  <done>Send is optimistic and idempotent, retries are silent, failure surfaces on exactly one bubble with the locked copy, and the visitor's typed text is never lost.</done>
</task>

<task type="auto">
  <name>Task 3: useChatStream — EventSource client with Last-Event-ID and silent reconnect</name>
  <files>src/lib/chat/useChatStream.ts</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-15/D-16/D-17 — the ~4-minute recycle produces no UI, polling fallback ships with the client switch off, "Reconnecting…" only after repeated CONSECUTIVE failures)
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (loading/sse-reconnect row; Copywriting Contract's "Reconnecting…" copy)
  </read_first>
  <action>
    Create src/lib/chat/useChatStream.ts: a client hook wrapping the browser's built-in EventSource against /api/chat/stream. Rely on EventSource's native Last-Event-ID behavior (it automatically resends the last received event id on reconnect — do not hand-roll a separate cursor-tracking mechanism). On the 'message' event type, parse and append to the caller's message list (deduplicating by message id, since a reconnect could in principle redeliver something already rendered — CHAT-07's no-duplicate guarantee is enforced server-side per Plan 01-08, but the client dedupes defensively by id). On the 'presence' event type, forward to the usePresence state from Plan 01-09 (both hooks operate on the same EventSource instance — expose the raw EventSource or an event-emitter so Plan 01-12's page-wiring can connect useChatStream's presence events to usePresence's state without either hook file needing to import the other). Track consecutive connection-error counts via EventSource's onerror; the hook exposes isReconnecting: boolean that only flips true after several consecutive errors (not on every single one) and flips back false on the next successful message/open — this is what drives the "Reconnecting…" line, which must NOT appear during the routine ~4-minute D-15 recycle (a clean close followed by an immediate successful reconnect is not an error and must not increment the counter). Export a POLLING_FALLBACK_ENABLED constant hardcoded to false (D-16 — the client switch stays off this phase; flipping it later is meant to be trivial).
  </action>
  <acceptance_criteria>
    useChatStream.ts does not hand-roll its own Last-Event-ID query-param tracking — it relies on the native EventSource reconnect behavior (no manual "lastEventId" header construction on the client for the primary transport).
    isReconnecting only becomes true after a threshold of consecutive errors greater than 1 (not on the very first error), verified by reading the counter-increment/reset logic.
    A message-id-based deduplication check exists before appending an incoming SSE message to the rendered list.
    POLLING_FALLBACK_ENABLED is defined as the literal boolean false.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "POLLING_FALLBACK_ENABLED" src/lib/chat/useChatStream.ts | grep -qE '^[1-9]' && grep -c "EventSource" src/lib/chat/useChatStream.ts | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>EventSource client dedupes by message id, silently rides the D-15 recycle, only shows "Reconnecting…" after repeated consecutive failures, and ships the polling fallback flag off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-generated client_msg_id | Client-controlled but only ever used for idempotent retry matching against the caller's own conversation — never trusted as an ordering or authorization signal server-side (enforced in Plan 01-08) |
| SSE message rendering | Message body is rendered as plain text via React's default JSX escaping — never dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-30 | Tampering (XSS) | src/components/chat/MessageBubble.tsx | high | mitigate | Message text is rendered as a plain text child, never via dangerouslySetInnerHTML or a Markdown/HTML renderer — React's default escaping is the sole and sufficient mitigation |
| T-01-31 | Denial of Service | src/components/chat/Composer.tsx retry loop | low | mitigate | Retry count and backoff are fixed and small (a bounded loop, not unbounded), so a persistent failure cannot spin indefinitely against the send endpoint |
</threat_model>

<verification>
Bidi isolation renders correctly for mixed-direction fixtures; delivery states are exactly sent/failed; retries never clear typed text; the EventSource hook dedupes and rides the deliberate recycle silently.
</verification>

<success_criteria>
CHAT-03's optimistic single-delivery-state contract and CHAT-04's live-arrival behavior are both real on the client, LANG-04's bidi isolation holds for mixed-direction content, and LANG-05's icon-mirror allowlist is respected — ready for Plan 01-12's final page wiring.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `src/components/chat/MessageBubble.tsx`, `MessageList.tsx`, `Composer.tsx`
- `src/lib/chat/useChatStream.ts`

<output>
Create `.planning/phases/01-foundation-and-the-realtime-spine/01-10-SUMMARY.md` when done
</output>
