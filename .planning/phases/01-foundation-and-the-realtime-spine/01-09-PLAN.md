---
phase: 01-foundation-and-the-realtime-spine
plan: 09
type: execute
wave: 5
depends_on: ["01-05", "01-06", "01-08"]
files_modified:
  - src/components/chat/Header.tsx
  - src/components/chat/Gate.tsx
  - public/manifest.webmanifest
  - public/sw.js
  - next.config.ts
  - src/components/chat/LanguageSheet.tsx
  - src/components/chat/Welcome.tsx
  - src/components/chat/PresenceLine.tsx
  - src/lib/chat/usePresence.ts
autonomous: true
requirements: [CHAT-02, CHAT-05, CHAT-09, LANG-02, LANG-03, LANG-05, LANG-06]
must_haves:
  truths:
    - "The header renders exactly two interactive controls — the language control and the appearance toggle — and nothing else; toggling either control twice in a row leaves exactly two controls, never spawning a third or duplicating the header (CHAT-09)."
    - "On first visit (no messages yet), the chat canvas is never blank — the two-line client-rendered welcome fills the empty state instead of an empty message list (CHAT-02)."
    - "With zero prior messages and the owner offline, the visitor still sees the honest two-line welcome (owner-offline copy) and can still send — there is no disabled or blocked state (CHAT-05)."
    - "The welcome is exactly two lines — warmth, then an honest presence statement — with no third line and no 'who the owner is' preamble, and it re-renders instantly on a language switch since it is never a row in messages (D-05/D-08)."
    - "The presence status line is plain Label-weight text with no color and no dot, updates live over the existing SSE stream's presence event, and never claims the owner is present when they are not (D-06/D-07)."
    - "Only the UI-SPEC-allowlisted icons (SendHorizontal, ChevronLeft, RotateCcw) visually mirror under dir=rtl in this component set — Languages, Sun/Moon, Check, and X never mirror, and no blanket [dir=rtl] icon-mirroring CSS rule exists anywhere in these files (LANG-05)."
    - "Selecting a language in the bottom sheet calls PATCH /api/chat/prefs and the sheet closes reflecting the new language without a full page reload (LANG-02, consuming Plan 01-06's route)."
    - "The language bottom sheet lists all confirmed languages as endonyms (العربية, 中文, Kiswahili, etc.) in their own script — never English names (D-09)."
    - "Light/dark can be toggled from the header and the toggle calls PATCH /api/chat/prefs to persist it (LANG-06, consuming Plan 01-06's route)."
    - statement: "Ten (or nine) endonym rows in the bottom sheet capped at 70vh scroll internally on a 360x640 viewport with every row still meeting the 44px touch target."
      verification: backstop
    - statement: "Both welcome lines clear the fold at 360x640 in Arabic, Hindi, Chinese, Russian, and Spanish — the longest expansions in the confirmed language list."
      verification: backstop
    - statement: "Rapidly tapping the appearance toggle multiple times in quick succession settles on a single consistent final theme state with no flicker or torn UI."
      verification: backstop
  artifacts:
    - path: "src/components/chat/Header.tsx"
      provides: "Language control + appearance toggle, exactly two controls, 44px touch targets"
    - path: "src/components/chat/LanguageSheet.tsx"
      provides: "Bottom sheet (vaul/drawer), endonym list, D-09"
    - path: "src/components/chat/Welcome.tsx"
      provides: "Two-line client-rendered welcome, presence-aware line 2"
    - path: "src/components/chat/PresenceLine.tsx"
      provides: "Passive status line, no color/dot, live via SSE presence event"
    - path: "src/components/chat/Gate.tsx"
      provides: "Env-bypassed push-gate shell wrapper (PUSH_GATE=off this phase)"
  key_links:
    - from: "src/components/chat/LanguageSheet.tsx"
      to: "src/app/api/chat/prefs/route.ts"
      via: "PATCH fetch on language selection"
      pattern: "PATCH"
    - from: "src/components/chat/PresenceLine.tsx"
      to: "src/lib/chat/usePresence.ts"
      via: "consumes the presence SSE event type emitted by chat/stream/route.ts"
      pattern: "presence"
  prohibitions:
    - requirement_id: "CHAT-09"
      category: "values"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT implement typing indicators or read/'seen' receipts anywhere in this component set, this phase or ever — out of scope by product decision (PRD Out-of-Scope), not merely deferred."
    - requirement_id: "CHAT-05"
      category: "values"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT display a queue position, wait-time estimate, or ticket-system language in Welcome.tsx or PresenceLine.tsx — CHAT-05's explicit 'without a queue counter or ETA' constraint."
    - requirement_id: "LANG-05"
      category: "values"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT apply a blanket RTL icon-mirroring CSS rule (e.g. a global [dir=rtl] .icon { transform: scaleX(-1) } style) — only the UI-SPEC allowlisted icons may mirror, applied per-icon via an explicit class."
  assumptions: []
---

<objective>
Build the visitor chat shell components: the two-control header, the language bottom sheet (endonyms), the welcome + presence status line, and the push-gate/PWA scaffold. These are standalone components (no `page.tsx` wiring yet — Plan 01-12 composes them) so this plan can run in parallel with Plan 01-10's composer/message-list work without a file conflict.

Purpose: CHAT-02, CHAT-09, LANG-02/03/05/06, and CHAT-05's honest offline welcome are the most visually-locked surfaces in UI-SPEC.md — this plan turns that design contract into working, tested components.
Output: `components/chat/{Header,Gate,LanguageSheet,Welcome,PresenceLine}.tsx`, `lib/chat/usePresence.ts`, `public/manifest.webmanifest`, `public/sw.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-05-SUMMARY.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, Gate shell, and PWA scaffold</name>
  <files>src/components/chat/Header.tsx, src/components/chat/Gate.tsx, public/manifest.webmanifest, public/sw.js, next.config.ts</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Color section — accent reserved for exactly 3 things, none of which is header chrome; Icon mirroring allowlist table; Spacing Scale's 44px touch-target rule; Registry Safety — button/drawer/input/label/textarea only)
    .planning/phases/01-foundation-and-the-realtime-spine/01-RESEARCH.md (Architectural Responsibility Map's "Push gate shell" row — static env-flag bypass, no permission logic, no SW push handlers yet)
    .claude/CLAUDE.md (web-push/service worker section — hand-written sw.js, no next-pwa, no Serwist; sw.js cache-control no-cache header rule)
  </read_first>
  <action>
    Create src/components/chat/Header.tsx: a fixed-height header rendering exactly two shadcn Button-based controls at 44x44px minimum — a language control (Languages icon, aria-label "Change language" from the locale JSON, opens LanguageSheet) and an appearance toggle (Sun/Moon icon, aria-label switching between "Switch to dark appearance"/"Switch to light appearance" from locale JSON, calls PATCH /api/chat/prefs on click). Neither icon is in the mirroring allowlist — apply no mirror class to either. Create src/components/chat/Gate.tsx: a client component accepting children, checking an env-exposed bypass flag (e.g. NEXT_PUBLIC_PUSH_GATE_BYPASS, defaulting to bypassed/on for Phase 1 per the Architectural Responsibility Map) — when bypassed, render children directly with zero permission-request logic; document in a comment that Phase 2 replaces this with the real hard-block gate. Create public/manifest.webmanifest with name, short_name, icons, display: 'standalone', start_url — the fields Phase 2's iOS Add-to-Home-Screen flow needs, scaffolded now. Create public/sw.js as a hand-written service worker with skipWaiting/clients.claim only — no push or notificationclick handlers yet (those are Phase 2; do not add next-pwa or Serwist as a dependency). Add a headers() rule to next.config.ts setting Cache-Control: no-cache on /sw.js so the service worker remains updatable (per CLAUDE.md's explicit warning about shipping an un-updatable SW).
  </action>
  <acceptance_criteria>
    Header.tsx renders exactly two elements with an onClick/interactive role — a static count check (grep for Button or role="button" usage) confirms exactly 2, not 1 or 3.
    Neither icon in Header.tsx has a conditional RTL-mirror class applied (Languages and Sun/Moon are absent from the icon-mirror allowlist).
    Gate.tsx's bypass check is read from an environment variable, not hardcoded true with no flag at all.
    public/sw.js contains no reference to next-pwa or serwist in imports.
    next.config.ts's headers() function includes a rule for /sw.js with Cache-Control: no-cache.
  </acceptance_criteria>
  <verify>
    <automated>! grep -RiE "next-pwa|serwist" public/sw.js && grep -c "no-cache" next.config.ts | grep -qE '^[1-9]' && grep -c "aria-label" src/components/chat/Header.tsx | grep -qE '^[2-9]' && echo ok</automated>
  </verify>
  <done>Header has exactly two controls with correct aria-labels, no icon mirroring beyond the allowlist, the Gate is env-bypassed (not hardcoded open), and the PWA/SW scaffold exists without next-pwa/Serwist.</done>
</task>

<task type="auto">
  <name>Task 2: Language bottom sheet (D-09 endonyms)</name>
  <files>src/components/chat/LanguageSheet.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-09 — endonyms, not English names, bottom sheet)
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Registry Safety — drawer pulls vaul; Spacing Scale — 44px rows, xl top padding; long-text/overflow backstop rows for the language sheet)
    src/server/i18n/detect.ts (from Plan 01-05 — SUPPORTED_LANGUAGES)
  </read_first>
  <action>
    Create src/components/chat/LanguageSheet.tsx using the shadcn drawer component (vaul-backed, --rtl already applied at the project level) as a bottom sheet, capped at 70vh with internal scroll for the row list. Render one row per language in SUPPORTED_LANGUAGES, each showing the language's endonym (from a small endonym-name map keyed by language code, sourced from the locale JSON's own endonym key authored in Plan 01-05, e.g. العربية for ar, 中文 for zh, Kiswahili for sw — never an English language name). Each row is a full-width button at minimum 44px height. Selecting a row calls PATCH /api/chat/prefs with the chosen lang, then closes the sheet. The sheet's title text ("Choose your language") comes from the locale JSON's languageSheetTitle key.
  </action>
  <acceptance_criteria>
    LanguageSheet.tsx contains no hardcoded English language name string (e.g. no literal "Arabic", "Chinese", "Swahili" used as row label text) — row labels are sourced from an endonym map or the locale JSON.
    Selecting a row triggers a fetch/PATCH call to /api/chat/prefs.
    The sheet container has a max-height constraint referencing 70vh (or an equivalent viewport-relative cap).
  </acceptance_criteria>
  <verify>
    <automated>! grep -E '"Arabic"|"Chinese"|"Swahili"|"Hindi"|"Russian"' src/components/chat/LanguageSheet.tsx && grep -c "prefs" src/components/chat/LanguageSheet.tsx | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>The language sheet lists endonyms only, is scroll-capped at 70vh, and persists a selection via the prefs route.</done>
</task>

<task type="auto">
  <name>Task 3: Welcome, presence status line, and the presence SSE hook</name>
  <files>src/components/chat/Welcome.tsx, src/components/chat/PresenceLine.tsx, src/lib/chat/usePresence.ts</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-CONTEXT.md (D-05 through D-08 in full — client-rendered, never a messages row, two lines only, presence-driven line 2, quiet status line that must never read as a third control)
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md (Color — "the presence status line has no color and no dot"; Typography — Label role for the status line; Copywriting Contract's exact welcome/presence strings)
  </read_first>
  <action>
    Create src/lib/chat/usePresence.ts: a client hook that consumes the 'presence' event type from the already-open chat SSE stream (the same EventSource instance Plan 01-10's useChatStream manages — this hook exposes a small subscribable presence state that useChatStream's event dispatch feeds into, documented so Plan 01-10's wiring plan (01-12) connects them without either plan needing to modify the other's file) and exposes isOwnerOnline: boolean. Create src/components/chat/Welcome.tsx: renders exactly two lines from the active locale JSON — welcomeLine1 (constant warmth line) and welcomeLine2Online/welcomeLine2Offline chosen by usePresence's isOwnerOnline — with no third line, no heading, no "who I am" preamble. This is a client component that re-renders instantly when the locale changes (no server round trip). Create src/components/chat/PresenceLine.tsx: renders under the header as plain Label-weight (14px/400) text in --muted-foreground with explicitly no background color, no dot element, and no icon — just the same presence-derived text usePresence exposes, styled so it cannot visually read as a third header control or a colored status widget.
  </action>
  <acceptance_criteria>
    Welcome.tsx renders exactly two text lines sourced from locale JSON keys (welcomeLine1 plus one of welcomeLine2Online/welcomeLine2Offline) with no third rendered line of body text.
    PresenceLine.tsx contains no color/background style tied to online/offline state and no dot/circle element.
    usePresence.ts's exported state is derived from the SSE presence event, not from a separate polling call.
    Neither Welcome.tsx nor PresenceLine.tsx contains the words "queue", "wait time", or "ETA" in any rendered string.
  </acceptance_criteria>
  <verify>
    <automated>! grep -iE "queue|wait time|\\bETA\\b" src/components/chat/Welcome.tsx src/components/chat/PresenceLine.tsx && grep -c "welcomeLine1" src/components/chat/Welcome.tsx | grep -qE '^[1-9]' && echo ok</automated>
  </verify>
  <done>The welcome is exactly two honest lines, never a queue/ETA phrase; the presence line is colorless/dotless text live over SSE; usePresence is ready for Plan 01-12 to wire alongside the message stream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE presence event -> UI | Server-authoritative; the client never infers or overrides presence state locally |
| PATCH /api/chat/prefs call sites | Client-initiated, scoped to the caller's own cookie by the server (Plan 01-06) — these components trust the server's enforcement, not their own |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28 | Tampering | src/components/chat/Gate.tsx | medium | accept | The Phase 1 bypass flag is a deliberate, documented shell (Architectural Responsibility Map) — accepted because the real permission gate and its security surface ship in Phase 2; no visitor data is protected by this gate in Phase 1 |
| T-01-29 | Information Disclosure | public/manifest.webmanifest / sw.js | low | accept | Static, non-secret, publicly bundled assets by design |
</threat_model>

<verification>
Header has exactly two controls with no icon-mirror violations; language sheet lists endonyms only and persists via the prefs route; welcome/presence never leak queue/ETA language and never fabricate presence.
</verification>

<success_criteria>
CHAT-02, CHAT-09, CHAT-05 (welcome half), LANG-02/03/05/06, and the Phase 1 gate/PWA scaffold are all delivered as tested, standalone components ready for Plan 01-12's final page composition.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `src/components/chat/Header.tsx`, `Gate.tsx`, `LanguageSheet.tsx`, `Welcome.tsx`, `PresenceLine.tsx`
- `src/lib/chat/usePresence.ts`
- `public/manifest.webmanifest`, `public/sw.js`
- `next.config.ts` — `/sw.js` cache-control header rule (additive to Plan 01-01's config)

<output>
Create `.planning/phases/01-foundation-and-the-realtime-spine/01-09-SUMMARY.md` when done
</output>
