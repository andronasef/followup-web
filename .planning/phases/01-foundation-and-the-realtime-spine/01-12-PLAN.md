---
phase: 01-foundation-and-the-realtime-spine
plan: 12
type: execute
wave: 6
depends_on: ["01-09", "01-10"]
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [CHAT-01, CHAT-08]
must_haves:
  truths:
    - "Visiting the root URL with no cookie renders the full-screen chat directly — no intermediate route, marketing page, or menu is reachable before the chat (CHAT-01)."
    - "A visitor returning in the same browser (valid signed cookie) sees their prior message history rendered on load, never a fresh empty conversation (CHAT-08, page-composition half)."
    - "Every component built in Plans 01-09 and 01-10 (Gate, Header, LanguageSheet, Welcome, PresenceLine, MessageList, Composer) is composed into one coherent page in the exact order the UI-SPEC.md surfaces list implies: Gate wraps everything, Header + presence + welcome sit above the message list, Composer is pinned at the bottom."
    - "usePresence (01-09) and useChatStream (01-10) share one underlying EventSource connection against /api/chat/stream — this page does not open two separate SSE connections for one visitor."
  artifacts:
    - path: "src/app/page.tsx"
      provides: "The single public page — composes Gate, Header, LanguageSheet, Welcome, PresenceLine, MessageList, Composer around one shared useChatStream connection"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/chat/useChatStream.ts"
      via: "one EventSource instance, whose 'presence' events are forwarded into usePresence's state"
      pattern: "useChatStream"
    - from: "src/app/page.tsx"
      to: "src/server/repo/messages.ts"
      via: "server-side initial history fetch (repo.messages.since(conversationId, 0)) passed as the message list's initial state, so history renders before the SSE connection even opens"
      pattern: "since"
  prohibitions: []
  assumptions: []
---

<objective>
Wire the standalone chat components from Plans 01-09 and 01-10 into `src/app/page.tsx`, the one public route this entire product exposes. This is a small, interface-first wiring plan by design — the components themselves were built and tested independently in the prior wave specifically so this composition step is low-risk.

Purpose: CHAT-01's "no landing page, straight into the chat" property is only true once this composition exists — everything before this plan is components in isolation.
Output: The finished `src/app/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-09-SUMMARY.md
@.planning/phases/01-foundation-and-the-realtime-spine/01-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the visitor chat page</name>
  <files>src/app/page.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-UI-SPEC.md ("Surfaces in scope" list — the exact visual order: header, welcome/presence, message area, composer)
    src/components/chat/Gate.tsx, Header.tsx, LanguageSheet.tsx, Welcome.tsx, PresenceLine.tsx (from Plan 01-09)
    src/components/chat/MessageList.tsx, Composer.tsx, src/lib/chat/useChatStream.ts (from Plan 01-10)
    src/server/auth/visitor.ts (requireVisitor, from Plan 01-06)
  </read_first>
  <action>
    Make src/app/page.tsx a Server Component that calls requireVisitor() to resolve the visitor's conversation, then server-side fetches the initial message history via the same repo.messages.since(conversationId, 0) query the SSE backfill uses, so history is present in the very first HTML response rather than waiting on the client to open a stream. Render a client boundary composing, top to bottom: <Gate> wrapping everything; inside it, <Header/> (language + appearance controls, opens <LanguageSheet/>); <Welcome/> and <PresenceLine/> together; <MessageList initialMessages={...}/> seeded with the server-fetched history; <Composer/> pinned to the bottom with the safe-area padding. Instantiate exactly one useChatStream() call at this composition level (not inside multiple child components independently) and pass its message-append callback down to MessageList and its presence-event stream down to usePresence — this is the one place both Plan 01-09's and Plan 01-10's hooks are connected, satisfying the "one shared EventSource" requirement neither prior plan could enforce on its own since they were built as independent, non-conflicting files.
  </action>
  <acceptance_criteria>
    src/app/page.tsx imports and renders Gate, Header, LanguageSheet (via Header), Welcome, PresenceLine, MessageList, and Composer — all six/seven components from Plans 01-09/01-10 appear in this one file.
    useChatStream is instantiated exactly once in this file (grep count of the hook's invocation is 1, not 2+).
    MessageList receives server-fetched initial history as a prop, not an empty array awaiting the first client fetch.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "useChatStream(" src/app/page.tsx | grep -qE '^1$' && grep -c "Gate\|Header\|MessageList\|Composer" src/app/page.tsx | grep -qE '^[4-9]|^[1-9][0-9]' && echo ok</automated>
  </verify>
  <done>The root page renders the full chat with no intermediate route, seeded with real history, sharing exactly one SSE connection across the presence and message hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the composed page against a running dev stack</name>
  <files>src/app/page.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-the-realtime-spine/01-01-SUMMARY.md (the documented local run command: docker compose up -d db && npm run dev)
  </read_first>
  <action>
    Run the full local stack (docker compose up -d db, migrations already applied from Plan 01-01, npm run dev) and load http://localhost:3000 in a fresh browser context (no cookie). Confirm: the page renders the chat directly with no redirect to any other route; exactly two header controls are visible; the welcome renders two lines; sending a message produces an optimistic bubble that settles to sent; reloading the page (simulating a return visit) shows the same conversation's history rather than a fresh empty one. Fix any composition-level wiring bug surfaced by this smoke test (this task exists specifically to catch integration issues that unit-level component tests in Plans 01-09/01-10 could not, since those plans tested components in isolation).
  </action>
  <acceptance_criteria>
    npm run build succeeds with the fully composed page.
    A manual smoke test (documented in the SUMMARY) confirms: direct chat render, two header controls, two-line welcome, working send, and history persistence across reload.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>The composed page builds and has been manually smoke-tested end to end against a real local stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root route (/) | The one fully public, unauthenticated surface of the entire product — must never leak owner-scoped data or admin controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-34 | Information Disclosure | src/app/page.tsx | high | mitigate | The server-side initial history fetch is scoped to the requireVisitor()-resolved conversation id only — never accepts a conversationId from a query param or client input at this route, so one visitor's page can never render another visitor's history |
</threat_model>

<verification>
`npm run build` succeeds; a manual smoke test against the local dev stack confirms direct-render, correct header, welcome, working send, and history persistence.
</verification>

<success_criteria>
CHAT-01 and the page-composition half of CHAT-08 are satisfied — the product's single public surface is fully wired and has been exercised against a real running stack, not just unit-tested components.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `src/app/page.tsx` (final composition, supersedes Plan 01-01's placeholder)

<output>
Create `.planning/phases/01-foundation-and-the-realtime-spine/01-12-SUMMARY.md` when done
</output>
